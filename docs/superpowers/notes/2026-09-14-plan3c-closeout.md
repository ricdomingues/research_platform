# Plano 3C (Dashboard, Portfólio, Compose de Produção e Ponta a Ponta) — Encerramento

- **Plano:** `docs/superpowers/plans/2026-09-14-virtual-order-engine-dashboard.md`
- **Spec:** v1.2 (`spec/virtual-order-engine-v1.2`) + D5–D39 (Planos 2, 3A, 3B) + D40–D59 (este plano)
- **Intervalo:** base `plan-3b-worker` (`0fd2707`) até a ponta do branch `plan-3c-dashboard` / tag `plan/virtual-order-engine-dashboard-complete` — 25 commits (24 de implementação, correções de revisão e testes + este encerramento)
- **Testes:** 1128 passando, 0 skips, 0 warnings, medidos do zero (`docker compose -f docker-compose.test.yml down -v && up -d --wait`, depois `uv run pytest -p no:cacheprovider -o addopts="" -q`) no commit `1e2d6de`; 908 herdados (`N_BASE`, baseline do Plano 3B medida em `8da0f24`, Task 0 deste plano); dashboard 47 passando, 0 skips, 0 warnings; a chamada única que mistura raiz, `api`, `worker`, `integration`, `evaluator` e `deploy` (`test_import_boundaries.py tests/integration/api/test_health_api.py tests/integration/api/test_dashboard_contract.py tests/integration/worker/test_runner.py tests/integration/test_quality_recheck.py tests/evaluator tests/deploy`) passa com 451 testes.
- **Núcleo congelado:** `git diff plan/virtual-order-engine-core-complete -- src/core` vazio e `git status --porcelain -- src/core` vazio — checados no Step 2, no commit final da verificação (`1e2d6de`).
- **Versões:** Streamlit 1.63.0, Plotly 6.9.0, uv 0.12.3 (mesma versão nas duas imagens).

## Critérios de aceite

| # | Critério | Teste | Resultado |
|---|---|---|---|
| 1 | Dashboard só por HTTP com `X-API-Key`, em projeto `uv` próprio; nenhum import de `core`/`virtual_orders`; `src` e `tests` da raiz não importam o dashboard; lock da raiz sem Streamlit/Plotly | `test_dashboard_talks_to_the_platform_only_over_http`, `test_platform_and_core_never_import_the_dashboard_or_ui_libraries`, `test_root_tests_never_import_the_dashboard_or_ui_libraries`, `test_boundary_scan_covers_the_dashboard_project`, `test_boundary_scan_covers_the_3c_modules` (`tests/test_import_boundaries.py`); `test_configuration_comes_from_the_environment_and_never_shows_the_key` (`dashboard/tests/test_client.py`) | OK |
| 2 | Visão geral: curva de R acumulado, cards com IC, risco de sequência, avisos e excluídas por revisão | `test_metric_cards_show_values_and_confidence_intervals`, `test_cumulative_r_follows_the_metrics_review_policy_and_order` (`dashboard/tests/test_viewmodels.py`); `test_overview_shows_cards_warnings_and_review_exclusions` (`dashboard/tests/test_app_smoke.py`) | OK |
| 3 | Sinais do dia com "Comprar virtual" e exibição explícita de 422 (`SIGNAL_EXPIRED`, `SIGNAL_NO_LONGER_ACTIONABLE` com motivo) e 503 (`ACTIONABILITY_UNVERIFIABLE`), sem traceback nem texto de exceção | `test_signal_and_order_rows_are_display_strings`, `test_api_errors_are_described_with_fixed_messages_only` (`dashboard/tests/test_viewmodels.py`); `test_buy_virtual_creates_a_manual_order`, `test_buy_virtual_errors_are_shown_explicitly_without_a_traceback` (`dashboard/tests/test_app_smoke.py`) | OK |
| 4 | Ordens: tabela filtrável; detalhe com candles, volume, VWAP, zona, stop, alvos, `evaluation_start_ts`, marcadores de eventos, qualidade (inclusive rechecks) e log | `test_order_chart_has_levels_markers_fill_model_and_a_bar_window` (`tests/integration/api/test_market_api.py`); `test_candlestick_figure_has_price_vwap_and_volume_rows_in_et`, `test_levels_zone_evaluation_start_and_markers_are_overlaid` (`dashboard/tests/test_charts.py`); `test_quality_view_shows_coverage_events_and_rechecks` (`dashboard/tests/test_viewmodels.py`); `test_orders_detail_page_renders_the_chart_quality_section_and_event_log` (`dashboard/tests/test_app_smoke.py`) | OK |
| 5 | Comparação: AUTO × MANUAL, estratégia, versão/configuração do fill model, `entry_path`, original × replay | `test_comparison_and_replay_pairs` (`dashboard/tests/test_viewmodels.py`); `test_comparison_page_renders_group_tables_and_a_replay_pair_with_its_chart` (`dashboard/tests/test_app_smoke.py`) | OK |
| 6 | Saúde: último ciclo, cobertura, `DATA_GAP`, fila `NEEDS_REVIEW` por motivo, incidentes, frozen, causas `INFO` `UNDELIVERABLE_ALERTS`/`ORDER_EVENT_ALERTS_BEHIND`, alertas `EXPIRED`, último `health_state_log` | `test_health_log_lists_transitions_newest_first`, `test_alert_outbox_lists_delivery_state_without_documents`, `test_quality_overview_reports_coverage_and_data_gaps` (`tests/integration/api/test_observability_api.py`); `test_health_view_surfaces_info_causes_review_queue_and_missing_runs` (`dashboard/tests/test_viewmodels.py`); `test_health_page_shows_info_causes_expired_alerts_and_the_last_health_log`, `test_health_page_shows_a_caption_when_the_log_is_empty` (`dashboard/tests/test_app_smoke.py`) | OK |
| 7 | Watchlist e regras sobre as rotas do 3B | `test_watchlist_page_lists_tickers`, `test_watchlist_page_shows_the_empty_message_only_when_the_call_actually_succeeded` (`dashboard/tests/test_app_smoke.py`) | OK |
| 8 | Painel de mercado: candles as-of com volume e VWAP de sessão, sobreposição de ordem, pressão sempre com `method` e `disclaimer`, sem chamada a provider | `test_bars_are_stored_bars_as_of_now_with_a_session_anchored_vwap_and_no_provider_call`, `test_pressure_is_always_labelled_as_an_estimate` (`tests/integration/api/test_market_api.py`); `test_pressure_display_always_carries_method_and_disclaimer`, `test_market_helpers` (`dashboard/tests/test_viewmodels.py`); `test_market_page_always_labels_pressure_as_an_estimate` (`dashboard/tests/test_app_smoke.py`) | OK |
| 9 | Portfólio virtual por 1R (qty, entrada média, último fechamento, P&L e R não realizados, alocação) | `test_long_and_short_positions_are_marked_per_1r_with_allocation`, `test_a_position_without_a_stored_close_is_unmarked_and_left_out_of_totals` (`tests/analytics/test_portfolio.py`); `test_virtual_portfolio_marks_open_positions_at_the_last_stored_close_and_empties_when_closed` (`tests/integration/api/test_portfolio_api.py`) | OK |
| 10 | Portfólio real: só contrato e slot desligado, `PHASE_0_PENDING`, modelo da Fase 0 com 17 itens "não verificado", nunca somado ao virtual | `test_real_portfolio_is_unavailable_until_phase_0`, `test_a_failing_real_source_reports_only_its_type`, `test_a_working_real_source_reports_its_positions_and_never_mixes_totals_with_virtual` (`tests/integration/api/test_portfolio_api.py`); `test_portfolio_views_never_mix_real_and_virtual` (`dashboard/tests/test_viewmodels.py`); `test_portfolio_page_shows_the_phase_0_message_and_never_a_combined_total` (`dashboard/tests/test_app_smoke.py`) | OK |
| 11 | Compose de produção: `postgres`, `migrate` one-shot, `api` com healthcheck com chave, `worker` único com `restart` e `stop_grace_period` ≥ 120 s, `dashboard`; `.env.example` só com placeholders; validação estática sem docker | `test_the_production_stack_has_exactly_the_spec_services_plus_migrations`, `test_the_worker_is_one_restarting_instance_with_a_long_grace_period`, `test_migrations_run_once_before_the_api_and_the_worker_with_only_the_database_url`, `test_the_api_runs_the_app_factory_and_its_healthcheck_reads_the_key_from_the_environment`, `test_env_example_lists_every_setting_with_placeholders_only_and_loads` (`tests/deploy/test_compose.py`) | OK |
| 12 | Ponta a ponta pela API: sinal → ordens → eventos → projeção → portfólio → métricas esperadas → `REPRODUCE` idêntico | `test_recorded_session_end_to_end_through_the_api` (`tests/integration/api/test_end_to_end_api.py`) | OK |
| 13 | Endurecimentos do 3B: M4 (código 4), sincronização da flag `stopping`, M2 (keepalives), ramo do lock liberado, M1 (janela), T9 (leitura limitada), M3 (paridade), T13 (isolamento), T10 (`0004` e as duas lacunas de teste), `PROVIDER_FAILURE_FINAL` com `NEEDS_REVIEW`, entrada 20 (nenhum import de `conftest`) | `test_database_down_at_start_logs_only_the_type_and_exits_4`, `test_shutdown_guard_is_claimed_exactly_once_across_threads` (`tests/worker/test_runner_startup.py`); `test_engine_sets_connect_timeout_utc_and_libpq_keepalives` (`tests/storage/test_database.py`); `test_behind_only_counts_events_inside_the_behind_window` (`tests/integration/test_alert_outbox.py`); `test_missing_runs_read_only_sessions_inside_the_checked_range` (`tests/integration/api/test_health_hardening.py`); `test_watchlist_and_live_cycle_share_the_session_window`, `test_3c_market_zone_copies_stay_equal` (`tests/worker/test_window_parity.py`); `test_an_unexpected_ingest_error_is_isolated_to_its_ticker` (`tests/integration/test_watchlist_alerts.py`); `test_a_second_recheck_row_for_the_same_order_and_session_is_rejected`, `test_downgrade_to_0003_and_back` (`tests/integration/test_schema_0004.py`); `test_a_session_evaluated_by_end_of_day_after_the_pending_read_is_left_alone` (`tests/integration/test_quality_recheck.py`); `tests/evaluator/test_recheck_levels.py`; `test_provider_failure_after_the_lookback_gets_a_final_row_a_review_and_clears_the_pending` (`tests/integration/test_quality_recheck.py`); `test_no_module_imports_a_conftest` (`tests/test_import_boundaries.py`) | OK |
| 14 | `src/core` intocado; `N_BASE` testes herdados verdes; migrations verificadas do zero; ruff e mypy strict limpos com `dashboard` | `git diff`/`git status --porcelain -- src/core` (Step 2, vazios); suíte completa de 1128 (inclui os 908 herdados); `uv run ruff check src tests migrations` e `uv run mypy` (110 arquivos) limpos; `uv run --directory dashboard ruff check .` e `uv run --directory dashboard mypy` (15 arquivos) limpos | OK |
| 15 | Commits com identidade noreply e sem trailers de IA; nenhuma tag com revisão aberta; nenhum push | `git log --format='%an <%ae>' plan-3b-worker..HEAD` (um único autor, `Ricardo Carneiro <132141856+ricdomingues@users.noreply.github.com>`); `git log --format='%B' plan-3b-worker..HEAD \| grep -E '^(Co-Authored-By\|Claude-Session):'` (nenhum resultado, Step 2) | OK |
| 16 | Contrato API × dashboard: respostas reais gravadas e comparadas por forma; view-models, cliente e figuras afirmados contra elas | `test_dashboard_contract_matches_the_recorded_api_responses` (`tests/integration/api/test_dashboard_contract.py`); `test_the_client_unwraps_every_recorded_list_envelope`, `test_overview_cards_curve_and_comparison_from_recorded_metrics`, `test_order_tables_detail_quality_and_chart_from_recorded_orders`, `test_market_portfolio_watchlist_comparison_and_health_from_recorded_responses` (`dashboard/tests/test_api_contract.py`) | OK |
| 17 | Streamlit sem detalhes de erro (`showErrorDetails none` sondado), dashboard em `127.0.0.1` com túnel SSH, imagens separadas por lock | `test_error_details_can_be_hidden_with_the_none_value` (`dashboard/tests/test_toolkit.py`); `test_the_dashboard_gets_only_the_api_url_and_key_and_hides_error_details`, `test_ports_bind_to_localhost_only_and_postgres_is_not_published`, `test_both_images_are_built_from_their_own_lock_without_dev_dependencies_as_a_non_root_user` (`tests/deploy/test_compose.py`) | OK |

Um nome de teste do brief precisou de ajuste na tabela acima: as linhas 1, 6, 9, 13 e 17 do brief citam arquivos sem nomear funções soltas (`test_market_api.py`, `test_observability_api.py`, `test_portfolio.py`, `test_toolkit.py`, entre outros); os nomes de função acima vieram de grep direto nesses arquivos e batem com o comportamento descrito em cada critério. Nenhum teste citado nominalmente pelo brief tinha um nome diferente do real.

## Rastreio das entradas 1–20 do 3B

| # | Entrada (nota de encerramento do 3B) | Commit(s) | Teste(s) |
|---|---|---|---|
| 1 | Compose de produção com `api`, `worker`, `dashboard`, `postgres`; worker único com `restart`/`stop_grace_period` ≥ 120 s; healthcheck do `api` | `0e27c64`, `a1a2984` | `tests/deploy/test_compose.py` (`test_the_production_stack_has_exactly_the_spec_services_plus_migrations`, `test_the_worker_is_one_restarting_instance_with_a_long_grace_period`, `test_the_api_runs_the_app_factory_and_its_healthcheck_reads_the_key_from_the_environment`) |
| 2 | Rotas de candles as-of (volume/VWAP) e sobreposição de ordens; pressão sempre com `method`/`DISCLAIMER` | `a7aab15` (VWAP/portfólio puros), `e4439f0` (rotas de mercado), `c03d912`/`be35296`/`17f510c` (cliente/view-models/tela) | `tests/analytics/test_vwap.py`, `tests/integration/api/test_market_api.py`, `dashboard/tests/test_charts.py`, `dashboard/tests/test_app_smoke.py::test_market_page_always_labels_pressure_as_an_estimate` |
| 3 | Telas da watchlist e das regras sobre as rotas da Task 15 (3B) | `17f510c` | `dashboard/tests/test_app_smoke.py::test_watchlist_page_lists_tickers`, `test_watchlist_page_shows_the_empty_message_only_when_the_call_actually_succeeded` |
| 4 | Dashboard exibindo causas `INFO` (`UNDELIVERABLE_ALERTS`, `ORDER_EVENT_ALERTS_BEHIND`), alertas `EXPIRED` e último `health_state_log` | `c6a6726` (rotas de observabilidade), `be35296`/`17f510c` (view-models/tela) | `tests/integration/api/test_observability_api.py`, `dashboard/tests/test_viewmodels.py::test_health_view_surfaces_info_causes_review_queue_and_missing_runs`, `dashboard/tests/test_app_smoke.py::test_health_page_shows_info_causes_expired_alerts_and_the_last_health_log` |
| 5 | Recuperação de jobs perdidos (D38): CLI ou decisão explícita de manter manual | decisão registrada (D53), sem código novo; runbook `0e27c64` | `docs/superpowers/runbooks/2026-09-14-compose-producao.md` § "Jobs perdidos (D53 — recuperação manual)" |
| 6 | Rever se o colapso de revisões por (ordem, motivo, pregão) da D33 é suficiente | **descartada — motivo (D55):** só dados reais de paper decidem; vira entrada 2 do próximo plano | — |
| 7 | Portfólio virtual; portfólio real da Robinhood somente leitura, condicionado à Fase 0 | `a7aab15` (marcação pura), `a61d7b7` (read model/rotas), `17f510c` (tela) | `tests/analytics/test_portfolio.py`, `tests/integration/api/test_portfolio_api.py`, `dashboard/tests/test_viewmodels.py::test_portfolio_views_never_mix_real_and_virtual`, `dashboard/tests/test_app_smoke.py::test_portfolio_page_shows_the_phase_0_message_and_never_a_combined_total` |
| 8 | Dashboard exibindo `data_quality.rechecks` do detalhe da ordem | `e4439f0` (rota do gráfico da ordem), `be35296`/`17f510c` | `tests/integration/api/test_market_api.py::test_order_chart_has_levels_markers_fill_model_and_a_bar_window`, `dashboard/tests/test_viewmodels.py::test_quality_view_shows_coverage_events_and_rechecks` |
| 9 | Pendências `PROVIDER_FAILURE` que persistem além de 20 runs `END_OF_DAY`: decidir status terminal | `4300c06` | `tests/integration/test_quality_recheck.py::test_provider_failure_after_the_lookback_gets_a_final_row_a_review_and_clears_the_pending`, `test_provider_failure_during_the_recheck_follows_d12` |
| 10 | Burst de cruzamentos ao ligar o webhook no meio do pregão | **descartada — motivo (D55):** só dados reais de paper decidem; vira entrada 2 do próximo plano | — |
| 11 | **M1** `ORDER_EVENT_ALERTS_BEHIND` sem idade máxima | `fdee5f5` | `tests/integration/test_alert_outbox.py::test_behind_only_counts_events_inside_the_behind_window`, `test_events_older_than_the_lookback_are_not_alerted_and_surface_as_behind` |
| 12 | **M2** keepalive/timeout na conexão do lock do worker | `185d7c7` | `tests/storage/test_database.py::test_engine_sets_connect_timeout_utc_and_libpq_keepalives` |
| 13 | **M3** paridade `watch_session`/`live_session` sem teste de igualdade | `fdee5f5` | `tests/worker/test_window_parity.py::test_watchlist_and_live_cycle_share_the_session_window`, `test_duplicated_window_constants_stay_equal`, `test_3c_market_zone_copies_stay_equal` |
| 14 | **M4** traceback bruto na subida do worker sem banco | `185d7c7` | `tests/worker/test_runner_startup.py::test_database_down_at_start_logs_only_the_type_and_exits_4` |
| 15 | **T9** `_JOB_RUNS` sem limite | `fdee5f5` | `tests/integration/api/test_health_hardening.py::test_missing_runs_read_only_sessions_inside_the_checked_range` |
| 16 | **T10** `UNIQUE(order_id, session_date)` (migration 0004) e as duas lacunas de teste do recheck | `4300c06` | `tests/integration/test_schema_0004.py::test_a_second_recheck_row_for_the_same_order_and_session_is_rejected`, `test_downgrade_to_0003_and_back`; `tests/integration/test_quality_recheck.py::test_a_session_evaluated_by_end_of_day_after_the_pending_read_is_left_alone`; `tests/evaluator/test_recheck_levels.py` |
| 17 | **T12** lista branca de payload, varreduras sem limite, trava contra entregadores concorrentes do outbox | **descartada — motivo (D55):** payloads do núcleo já estruturados; volumes de um único responsável; worker único (D20/D39) contra concorrência; varreduras já têm `LIMIT`/lookback | — |
| 18 | **T13** aborto do ciclo inteiro da watchlist por exceção inesperada de ingestão | `eeec6b2` | `tests/integration/test_watchlist_alerts.py::test_an_unexpected_ingest_error_is_isolated_to_its_ticker` |
| 19 | **T16** `shutdown(wait=False)` com jobs em andamento; desigualdade de cadência `*/N`; ramo "vivo mas lock perdido" não testado | ramo de teste coberto em `185d7c7`; `shutdown(wait=False)` e `*/N` **descartados — motivo (D55):** desenho intencional da D39/D36 | `tests/worker/test_runner_startup.py::test_shutdown_guard_is_claimed_exactly_once_across_threads` |
| 20 | Erro de escopo do `conftest` do pytest ao misturar arquivos de teste em diretórios diferentes | `493083e` | `tests/test_import_boundaries.py::test_no_module_imports_a_conftest`; chamada mista de 451 testes no Step 2 (regressão) |

## Decisões D40–D59

- **D40** (dashboard só por HTTP: projeto próprio, `client.py`/`viewmodels.py`/`charts.py`/`views/*.py`/`app.py`; navegação de entrada única por `st.sidebar.radio`, sem `pages/`; erros via `guarded`/`describe_api_error`) — mantida, Tasks 1, 11–13 — custo se estivesse errada: uma pasta `pages/` ligaria multipage sem querer, ou um erro vazaria texto de exceção na tela.
- **D41** (leituras "atuais" do dashboard são as-of `data_as_of` obtido no instante da requisição, devolvido na resposta, sem run/evento/snapshot) — mantida, Tasks 6, 7, 8 — custo se estivesse errada: o dashboard criaria efeito colateral (run, evento) só por ser lido.
- **D42** (rotas novas aditivas: `/market/bars`, `/market/pressure`, `/orders/{id}/chart`, `/portfolio/virtual`, `/portfolio/real`, `/health/log`, `/alert-outbox`, `/quality/overview`) — mantida, Tasks 7, 8, 9 — custo se estivesse errada: quebra de compatibilidade com as 14 rotas do 3A/3B.
- **D43** (VWAP de sessão `SESSION_VWAP_TYPICAL_PRICE_V1`, reiniciada por data ET, lida desde 00:00 ET do dia de `from`) — mantida, Task 6 — custo se estivesse errada: VWAP do gráfico divergiria da VWAP real da sessão, ou a janela pedida cortaria a abertura.
- **D44** (pressão: janela de `window_bars` (5..390) até `data_as_of`; `side` só com `cmf_threshold`; sempre `estimate`/`method`/`disclaimer`) — mantida, Task 7 — custo se estivesse errada: um alerta de pressão passaria por sinal real, não estimativa.
- **D45** (portfólio virtual por 1R: posições `OPEN`/`PARTIAL`, marcação as-of, `unrealized_r`/`open_r`, alocação por notional, posição sem candle fora dos totais, `ROUND_HALF_EVEN`) — mantida, Task 6, 8 — custo se estivesse errada: o responsável confundiria a curva por 1R com dinheiro real de conta.
- **D46** (portfólio real: só contrato `PortfolioSource`/`RealPosition`, `Services.portfolio_source: None`, `PHASE_0_PENDING`, nunca somado ao virtual) — mantida, Task 8, ruling do controlador — custo se estivesse errada: um total combinado enganaria o responsável sobre o tamanho real da posição antes da Fase 0 existir.
- **D47** (Compose de produção: duas imagens por lock próprio; `migrate`/`api`/`worker` reusam a imagem do motor com `pull_policy: never`; portas só em `127.0.0.1`) — mantida, Task 14 — custo se estivesse errada: o Compose construiria a imagem do motor duas vezes, ou publicaria uma porta acessível de fora.
- **D48** (healthcheck do `api` com `X-API-Key` do ambiente, sem `/livez`) — mantida, Task 14 — custo se estivesse errada: um `/livez` sem banco não pegaria migração pendente; isentar uma rota da chave quebraria a D19.
- **D49** (endurecimentos do worker: M4 código 4, `ShutdownGuard.claim()` atômico, M2 keepalives/`tcp_user_timeout`) — mantida, Task 2 — custo se estivesse errada: um segundo sinal derrubaria o scheduler de forma não graciosa, ou uma conexão meio-aberta travaria o `worker_lock` indefinidamente.
- **D50** (`/health`: M1 janela de 7 dias para `ORDER_EVENT_ALERTS_BEHIND`, T9 âncora própria bounded, M3 teste de paridade) — mantida, Task 3 — custo se estivesse errada: `_JOB_RUNS`/varreduras cresceriam sem limite num banco grande.
- **D51** (watchlist isola exceção inesperada por ticker em `_ingest`, run `FAILED` continua gravando `repr(exc)`) — mantida, Task 4 — custo se estivesse errada: um ticker com erro abortaria a ingestão de todos os outros no mesmo ciclo.
- **D52** (migration 0004 `UNIQUE(order_id, session_date)`; `PROVIDER_FAILURE` com ≥5 pregões fechados vira `PROVIDER_FAILURE_FINAL` e marca `DATA_QUALITY_UNVERIFIED`; `NO_OBSERVATIONS_FINAL` continua sem revisão) — mantida, Task 5 — custo se estivesse errada: uma pendência de qualidade sumiria em silêncio da janela de 20 runs, ou (Minor 4 da revisão final) uma única falha transitória depois de uma parada longa do worker finalizaria a ordem sem retentativa prévia — efeito conservador, registrado como entrada 7 do próximo plano.
- **D53** (recuperação de jobs perdidos continua manual; sem comando de CLI) — mantida, Task 14 (runbook) — custo se estivesse errada: rodar a abertura/fim de dia fora de hora quebraria a D4/D12 (fotografia do job, dividendos fora da janela).
- **D54** (ponta a ponta pela API com fonte falsa gravada e jobs reais, cobrindo `REPRODUCE` idêntico) — mantida, Task 10 — custo se estivesse errada: uma regressão de integração entre rotas só apareceria em produção.
- **D55** (entradas adiadas/descartadas do 3B com motivo: 6, 10, 17, partes da 19 descartadas; lacunas de teste do T10 cobertas na Task 5; entrada 20 corrigida na Task 1; role de banco sem `UPDATE`/`DELETE` como desvio registrado da spec 6) — mantida — custo se estivesse errada: o Plano 3C trataria itens que só dados reais de paper podem decidir, sem base para a decisão.
- **D56** (dashboard é projeto `uv` próprio, ruling do controlador; `streamlit>=1.40,<2`, `plotly>=5.24,<7`; lock da raiz sem essas libs) — mantida, Task 1 — custo se estivesse errada: um `uv add` na raiz rebaixaria pandas/numpy/protobuf/click/packaging/tornado sob o núcleo congelado.
- **D57** (contrato API × dashboard por fixtures gravadas: `test_dashboard_contract.py` na raiz não importa o dashboard; `test_api_contract.py` no dashboard consome as mesmas fixtures) — mantida, ruling do controlador, Task 12 — custo se estivesse errada: uma mudança de chave na API quebraria o dashboard em produção sem nenhum teste acusando.
- **D58** (configuração do contêiner do dashboard: `client.showErrorDetails` texto (`none`), sondado na Task 1 — não veio booleano, então não houve necessidade do `Ruling:` de fallback previsto; `dashboard` depende de `api` com `service_started`) — mantida, Tasks 1, 14 — custo se estivesse errada: um alias legado removido faria o Streamlit recusar a opção na subida do Compose.
- **D59** (limites aceitos: sem autenticação própria — `127.0.0.1` + túnel SSH; curva de R limitada a 1000 ordens fechadas) — mantida, Task 14 — custo se estivesse errada: mais de 1000 ordens fechadas omitiriam as mais antigas da curva sem aviso (mitigado pela legenda de truncamento).

## Rulings durante a execução

1. **Task 0 não despachada.** `N_BASE = 908` passando, 0 skips, 0 warnings, medido do zero em `8da0f24` (a verificação final do Plano 3B); `0fd2707` e `df9e010` diferem só em documentação, então a base efetiva do Plano 3C é a mesma árvore. Custo se estivesse errada: a linha de base da Task 1 ficaria errada, mas a própria corrida completa da Task 1 pegaria a discrepância.
2. **Task 1 — sondas da API instalada casaram com o esperado, sem `Ruling:` de contingência.** Streamlit 1.63.0, Plotly 6.9.0, pandas 3.0.5 (lock da raiz intocado); `client.showErrorDetails` já é texto na versão instalada, com `"none"` aceito — o caminho primário da D58 se aplicou direto, sem precisar do fallback booleano `false` previsto no plano. O `AppTest` também bateu com o comportamento esperado sem adaptação de código, só de expectativa de teste. Custo se estivesse errado: nenhum — era só uma sonda de compatibilidade.
3. **Task 9 — arredondamento canônico do portfólio resolvido pelo controlador.** `CANONICAL_CONTEXT = Context(prec=28, rounding=ROUND_HALF_EVEN)` confirmado como o contexto correto para dinheiro (2 casas) e razões (4 casas) do D45, batendo com o arredondamento já pedido pelo brief. Custo se estivesse errado: dinheiro e razões do portfólio virtual arredondariam de forma inconsistente com o resto da plataforma.
4. **Task 11 — Important mandada pelo plano: cliente aceitava NaN/Infinity na leitura e podia emitir NaN na escrita.** Corrigido na hora: rejeição por `parse_constant` na leitura (código fixo, sem texto de exceção) e `allow_nan=False` na escrita, espelhando `api/encoding.py`. Custo se estivesse errado: nenhum.
5. **Task 12 — Important mandada pelo plano: fixtures gravadas não eram determinísticas** (timestamps de relógio de parede, UUIDs aleatórios). Corrigido na hora, em modo de gravação: UUIDs viram placeholders estáveis numerados por primeira aparição, consistentes entre todos os arquivos; timestamps não derivados do relógio falso viram sentinela fixa; texto decimal preservado ao gravar fixtures; asserções fracas do contrato apertadas para valores exatos; motivo de portfólio real ausente sem "None" no texto renderizado. Essa mesma rodada tratou a normalização determinística incluindo campos `*_hash` (hashes de payload/config) e o empate de ordenação do `/portfolio/virtual` (mesmo ticker, UUID aleatório) — o empate ficou como achado menor adiado (a fixture canoniza a ordem; o dashboard não depende dela). Custo se estivesse errado: uma nova regravação de fixture.
6. **Task 13 — `functools.partial` nas views julgado equivalente ao desenho do plano.** A revisão de task e a revisão final confirmaram que o desvio (usar `functools.partial` para fechar `client`/`api` sobre os `render()` das views Streamlit) não muda comportamento nem fronteiras. Custo se estivesse errado: nenhum — era estilo de código, não semântica.
7. **Task 14 — Important mandada pelo plano: o runbook/comentário do Compose tratava a saída 2 do worker só como "lock ocupado"**, mas `EXIT_CONFIG` também é 2. Corrigido na hora, junto com um lote de achados menores baratos: `uv sync --locked` nos dois Dockerfiles, último `USER` sempre `vo`, `.dockerignore` do dashboard excluindo `.env`, extração da chave no runbook sem `source` do `.env` nem exposição em argv, `export GIT_SHA` antes de `compose config`; `docker compose build` rodado uma vez como evidência (não bloqueante se a rede faltasse). Custo se estivesse errado: mais um commit pequeno de correção.
8. **Revisão final — rodada de correção = 5 itens combinados.** Minor 1 (orientação de `DATABASE_URL` sobre percent-encoding/usuário/banco/`$`), Minor 2 (smoke `AppTest` de Ordens/Comparação apoiado em fixtures gravadas), o T8 adiado (`/portfolio/real` sucesso), Minor 7 (retorno antecipado da watchlist em chamada falha; legenda de log de saúde vazio) e Minor 8 (`.dockerignore` raiz com `**/.env`) — baratos, fixam o contrato futuro da Fase 0 e a correção do primeiro deploy. Custo se estivesse errado: nenhum.
9. **Revisão final — Minors 3–6 viram entradas do próximo plano, não correções agora.** `/quality/overview` ignora pregões resolvidos por `DATA_QUALITY_RECHECK` (Minor 3), `PROVIDER_FAILURE_FINAL`/`NO_OBSERVATIONS_FINAL` podem finalizar na primeira tentativa depois de uma parada longa do worker (Minor 4), o gráfico da ordem é montado de duas leituras separadas (Minor 5) e marcadores caem dentro do intervalo de horas ocultas do gráfico (Minor 6). Custo se estivesse errado: o responsável só veria esses efeitos na observação em paper primeiro.

## Resultado da revisão final

Revisão de branch inteiro (opus, `0fd2707..db9a883`): **Ready to merge** — 0 Critical, 0 Important, 8 Minor.

Rodada de correção única (commits `db9a883..1e2d6de`): Minor 1 (`8c0e905`, orientação de `DATABASE_URL`), Minor 8 (`8c0e905`, `.dockerignore` raiz), Minor 2 (`4c6a55f`, smoke `AppTest` de Ordens/Comparação com fixtures), Minor 7 (`4c6a55f`, watchlist/health vazios), T8 adiado (`1e2d6de`, `/portfolio/real` sucesso). Resultado: 1128 passando, 0 skips, 0 warnings (root, +1); dashboard 47 passando, 0 skips, 0 warnings (+4).

Re-revisão de escopo restrito (sonnet, sem testes novos): os 5 achados foram todos marcados ADDRESSED, sem quebra nova; nenhuma mudança em `src/`; os dois locks continuaram inalterados.

## Achados menores adiados

**Adiados para o Plano 3C** (triagem da revisão final, item por item):
- Task 3 (teste de paridade sem data de transição de horário de verão): adiado — o calendário compartilhado torna a divergência improvável; acrescentar uma data de DST quando qualquer um dos módulos mudar de novo.
- Task 7 (ramos não testados das rotas de mercado; `source` desconhecido não validado; `msg` do pydantic em `REQUEST_INVALID`): adiado — os ramos são cobertos em parte pelas fixtures de contrato; um `source` desconhecido devolve resultado vazio/`NO_BARS`, inofensivo; o saneamento de `msg` pertence ao endurecimento do 3A e o dashboard já filtra.
- Task 9 (`/quality/overview` com limite inválido sem teste próprio): **descartado (drop)** — mesmo mecanismo `Query(ge, le)` de `/health/log`, já coberto por `REQUEST_INVALID` genérico.
- Task 11 (colisão de sentinela do `json_text`): **descartado (drop)** — os corpos são payloads de regra construídos pelo dashboard, sem texto livre que se iguale a `__decimal_N__`.
- Task 12 (comparador de forma: opaco por nome, `null` como curinga, compara só o primeiro elemento de lista): adiado — é mandado pelo plano e documentado; os valores exatos afirmados em `test_api_contract.py` compensam.
- Task 12 (`/portfolio/virtual` empate no mesmo ticker ordenado por UUID aleatório): adiado — o dashboard não depende da ordem; ao tocar o código, usar `ORDER BY g.ticker, o.created_at, o.id` (entrada 11 do próximo plano).
- Task 14 (exclusão redundante de `DASHBOARD_API_URL` no teste de nomes de segredo): **descartado (drop)**.
- Task 1 (varreduras de fronteira por sistema de arquivos, não por arquivos versionados; `assert` morto de `.venv`; nome de `_dashboard_rel`; regra de carregar para T11-13): **descartado (drop)** — a regra de carregar foi cumprida (`DASHBOARD_FORBIDDEN` inclui `tests`, censo de import limpo).

**Registrados para o próximo plano, não corrigidos agora (Minors 3–6 da revisão final):** ver entradas 6 e 7 abaixo.

## Entradas para o próximo plano

1. Executar a Fase 0 da Robinhood e preencher `docs/superpowers/roadmap/2026-09-14-robinhood-phase0-feasibility-report.md`; só então planejar o adapter de `PortfolioSource`. **O portfólio real da Robinhood continua desligado até essa evidência existir** — hoje só o contrato e o slot `PHASE_0_PENDING` estão implementados (D46).
2. Observação em paper: D33 (colapso de revisões), burst de cruzamentos ao ligar o webhook (D26), itens do T12 (entradas 6, 10, 17 do 3B, descartadas por falta de dados reais).
3. Endurecimento de VPS: role de banco sem `UPDATE`/`DELETE` para `api`/`worker` (**desvio registrado da spec 6**, D55), TLS/proxy e autenticação na frente do dashboard (hoje limitação conhecida: só `127.0.0.1` + túnel SSH, D59), backup do volume `pgdata`.
4. Curva de R acumulado limitada a 1000 ordens fechadas (limite de `GET /orders`, legenda na tela, D59); paginar ou criar rota agregada quando passar disso.
5. Revisar em paper quantas ordens saem das métricas por `DATA_QUALITY_UNVERIFIED` (D52) e se `NO_OBSERVATIONS_FINAL` deveria marcar revisão.
6. `/quality/overview` ignora pregões resolvidos por `DATA_QUALITY_RECHECK` (Minor 3 da revisão final de branch do 3C): a cobertura e os `DATA_GAP` vêm só de `order_events`, então um fim de dia perdido e depois rechecado nunca aparece na tela de Saúde, só no detalhe da ordem.
7. `PROVIDER_FAILURE_FINAL`/`NO_OBSERVATIONS_FINAL` podem finalizar na primeira tentativa depois de uma parada longa (≥5 pregões) do worker ou do recheck (Minor 4): não há exigência de uma tentativa `PROVIDER_FAILURE` anterior antes de finalizar; avaliar se vale exigi-la.
8. O gráfico da ordem (`GET /orders/{id}/chart`) é montado a partir de duas leituras separadas sob READ COMMITTED (ordem/projeção e `stored_events`); um ciclo concorrente pode produzir marcadores fora da janela usada (Minor 5). Cosmético e autocurável no próximo carregamento; considerar uma única transação REPEATABLE READ se incomodar na prática.
9. Os `rangebreaks` do gráfico ocultam horas estendidas mas não feriados; marcadores dentro do intervalo oculto (por exemplo `evaluation_start_ts` ou um sinal criado antes das 09:30 ET) ficam invisíveis (Minor 6). Cosmético; considerar prender esses marcadores à abertura da sessão ou remover o `rangebreak` de hora nos gráficos de ordem.
10. O contêiner do dashboard só foi construído (`docker compose build`), nunca executado (`up`). No primeiro `docker compose up` real, confirmar que a página do dashboard carrega — checar a resolução de import: `streamlit run dashboard/app.py` executa o arquivo fonte, enquanto `dashboard.*` resolve para a cópia instalada (não editável) do pacote.
11. `/portfolio/virtual` ordena por `ticker`, mas o empate entre posições do mesmo ticker fica por UUID aleatório da ordem (achado menor adiado da Task 12); trocar para `ORDER BY g.ticker, o.created_at, o.id` quando o read model for tocado de novo.

## Nota de encerramento do 3B

`docs/superpowers/notes/2026-09-13-plan3b-closeout.md` — 20 entradas de próximo plano rastreadas na tabela acima.
