# Plano 3B (Worker, Alertas n8n, Watchlist e Endurecimento) — Encerramento

- **Plano:** `docs/superpowers/plans/2026-09-13-virtual-order-engine-worker.md`
- **Spec:** v1.2 (`spec/virtual-order-engine-v1.2`) + D5–D12 (Plano 2) + D13–D19 (Plano 3A) + D20–D39 (este plano)
- **Intervalo:** base `plan-3a-api` (`8cfb743`) até a ponta do branch `plan-3b-worker` / tag `plan/virtual-order-engine-worker-complete` — 23 commits (22 de implementação, correções de revisão e testes + este encerramento)
- **Testes:** 908 passando, 0 skips, 0 warnings, medidos do zero (`docker compose -f docker-compose.test.yml down -v && up -d --wait`, depois `uv run pytest -p no:cacheprovider -o addopts="" -q`) no commit `8da0f24`; 671 herdados (`N_BASE`, baseline do Plano 3A medida em `9cb2889`, Task 0 deste plano)
- **Núcleo congelado:** `git diff plan/virtual-order-engine-core-complete -- src/core` vazio e `git status --porcelain -- src/core` vazio — checados no Step 2, no commit final da verificação (`8da0f24`); a revisão final de branch inteiro também checou `git diff 8cfb743..2f57ed5 -- src/core` vazio

## Critérios de aceite

| # | Critério | Teste | Resultado |
|---|---|---|---|
| 1 | Worker agenda ciclo (a cada `EVAL_INTERVAL_MINUTES`, 09:30–16:05 ET, meio pregão e feriados respeitados), watchlist em job próprio na mesma cadência, abertura (09:25 ET) e fim de dia (16:30 ET, retry 18:30 só se não assentado); intervalo fora de 1..59 é `CONFIG_INVALID`; jobs testados sem tempo real; abertura/fim de dia ausentes aparecem em `/health` | `test_live_cycle_fires_every_interval_on_weekdays_between_9_and_16_et`, `test_opening_fires_at_9_25_et_on_weekdays`, `test_end_of_day_fires_at_16_30_and_18_30_et`, `test_watchlist_is_its_own_job_on_the_live_cadence`, `test_live_session_window_follows_the_nyse_calendar`, `test_interval_outside_the_cron_minute_range_fails_at_startup` (`tests/worker/test_schedule.py`); `tests/integration/worker/test_jobs.py`; `test_invalid_configuration_exits_2_naming_variables_only`, `test_run_rejects_an_interval_the_cron_cannot_express_before_building_services` (`tests/integration/worker/test_cli.py`); `test_missing_opening_and_end_of_day_runs_are_visible_after_their_grace` (`tests/integration/api/test_health_hardening.py`) | OK |
| 2 | Um worker por banco; lock vigiado e saída com código 3 ao perdê-lo; desligamento gracioso; `services.close()` e liberação do lock sempre | `test_a_second_worker_exits_without_scheduling`, `test_a_lost_worker_lock_alerts_and_stops_the_scheduler`, `test_termination_signals_shut_the_scheduler_down_gracefully`, `test_services_are_closed_and_the_lock_released_when_the_scheduler_fails`, `test_a_second_signal_does_not_shut_the_scheduler_down_twice`, `test_a_signal_after_the_lock_is_lost_does_not_shut_down_twice` (`tests/integration/worker/test_runner.py`) | OK |
| 3 | `Services.clock` só para o instante de mercado; `require_aware` em `evaluate_order`, `finalize_validity`, `expire_due_orders`; `market_now` em runs `FAILED` | `tests/integration/test_naive_clocks.py` (11 casos); `test_failed_cycle_records_its_market_now_and_releases_the_lock` (`tests/integration/test_cycle.py`); `test_unexpected_error_marks_the_opening_run_failed` (`tests/integration/test_opening.py`) | OK |
| 4 | `python -m virtual_orders.worker rebuild-projections` com resumo JSON e código de saída; isolamento por ordem; replay nunca congelada (M5) | `test_rebuild_projections_prints_a_summary_and_signals_problems_by_exit_code` (`tests/integration/worker/test_cli.py`); `test_tampered_replay_projection_records_an_incident_without_freezing_the_replay`, `test_tampered_live_projection_is_still_frozen`, `test_rebuild_all_isolates_unexpected_errors_and_unknown_orders`, `test_rebuild_all_without_ids_still_scans_every_order` (`tests/integration/test_rebuild_hardening.py`) | OK |
| 5 | `DATA_QUALITY_RECHECK` consome `QUALITY_NOT_EVALUATED` uma vez (uma linha por ordem e pregão), nunca escreve `DATA_QUALITY`/`DATA_GAP`, respeita D4 e D12, fecha pendências impossíveis com linha terminal e checa toque de nível só com os níveis vigentes no pregão | `test_recheck_consumes_a_session_skipped_by_d12_exactly_once`, `test_recheck_flags_reviews_but_never_writes_data_quality_or_data_gap`, `test_the_just_closed_session_is_left_to_end_of_day`, `test_a_session_outside_the_order_window_gets_a_terminal_row`, `test_no_observations_after_the_lookback_gets_a_final_row_and_clears_the_pending`, `test_level_touch_uses_the_stop_in_force_during_the_rechecked_session`, `test_level_touch_is_skipped_with_a_reason_when_levels_cannot_be_derived`, `test_a_second_recheck_row_for_the_same_session_is_never_written` (`tests/integration/test_quality_recheck.py`) | OK |
| 6 | `DIVIDEND_UNVERIFIED` quando as duas fontes levantam; respostas vazias continuam sem revisão | `test_both_sources_raising_flags_an_open_position_without_a_dividend_record`, `test_both_sources_answering_empty_is_still_no_dividend` (`tests/integration/test_dividend_unverifiable.py`) | OK |
| 7 | Webhook n8n opcional: fora do caminho crítico, timeout de 5 s por fase, orçamento por execução, backoff por alerta, expiração em 24 h registrada e visível como `INFO`, sem URL/segredo/texto de exceção | `test_posts_one_json_document_with_the_idempotency_key`, `test_non_2xx_raises_with_the_status_and_no_retry`, `test_timeout_raises_the_exception_type_only`, `test_repr_hides_the_url` (`tests/notify/test_n8n_webhook.py`); `test_a_failing_alert_backs_off_without_blocking_the_others`, `test_alerts_older_than_a_day_are_expired_not_deleted_and_surface_as_info`, `test_the_time_budget_leaves_the_rest_for_the_next_run`, `test_real_webhook_adapter_end_to_end_without_network` (`tests/integration/test_alert_outbox.py`) | OK |
| 8 | Resumo de fim de dia e alerta de `/health` por transição (estado, causa nova ou recuperação), sem repetir por ciclo nem após reinício | `test_transitions_are_logged_and_alerted_once_even_after_a_restart`, `test_return_to_healthy_sends_one_recovery_alert`, `test_end_of_day_summary_is_enqueued_once_per_cycle_run`, `test_disabled_webhook_still_logs_transitions_without_enqueueing` (`tests/integration/test_health_watch.py`) | OK |
| 9 | Alertas de eventos de ordem (enfileirados fora do ciclo live, revisões uma vez por ordem/motivo/pregão), de incidentes de integridade, de cruzamento de níveis e de pressão forte, uma vez por fato, idempotentes entre reinícios; pressão rotulada como estimativa; atraso além do lookback visível como `INFO` | `test_order_events_are_enqueued_once_reviews_collapse_per_session_and_replays_are_ignored`, `test_events_older_than_the_lookback_are_not_alerted_and_surface_as_behind`, `test_integrity_incidents_are_alerted_once_each_without_their_detail` (`tests/integration/test_alert_outbox.py`); `test_price_crossings_are_enqueued_once_with_cooldown`, `test_pressure_rule_is_labelled_as_an_estimate` (`tests/integration/test_watchlist_alerts.py`); `test_cooldown_suppresses_repeated_crossings_including_across_calls`, `test_pressure_alert_needs_a_full_window_and_respects_the_cooldown` (`tests/alerts/test_rules.py`) | OK |
| 10 | Watchlist + regras (migration 0003), ingestão de candles da watchlist sem fallback, rotas autenticadas no envelope do 3A | `test_alert_rule_checks_reject_inconsistent_definitions`, `test_removing_a_watchlist_ticker_removes_its_rules`, `test_outbox_keys_are_unique`, `test_recheck_keys_are_unique_per_order` (`tests/integration/test_schema_0003.py`); `test_ingestion_resumes_from_the_last_stored_bar_and_failures_are_recorded` (`tests/integration/test_watchlist_alerts.py`); `test_adding_a_ticker_checks_it_once_and_is_idempotent`, `test_untradable_is_422_and_unverifiable_is_503_without_storing`, `test_rules_are_created_listed_and_deleted_with_exact_decimals`, `test_removing_a_ticker_removes_its_rules` (`tests/integration/api/test_watchlist_api.py`) | OK |
| 11 | M4 (timeout ≠ indisponível), M6 (overrides contra a origem), M7 (404 sem corpo Alpaca → 503), `bootstrap` fecha o cliente | `test_slow_health_query_is_degraded_not_database_unavailable` (`tests/integration/api/test_health_hardening.py`, M4); `test_overrides_invalid_for_one_source_configuration_fail_before_the_run` (`tests/integration/test_recalculate_overrides.py`, M6); `test_404_without_an_alpaca_error_body_is_unavailable_not_unknown` (`tests/marketdata/test_alpaca_assets_404.py`, M7); `test_owned_client_is_closed_when_the_engine_cannot_be_built` (`tests/test_bootstrap_worker.py`) | OK |
| 12 | Testes faltantes das entradas 13 e 14 (ramo `FAILED` da abertura, sessões em `/health`, `market_now` em `FAILED`, revisão sem motivo, `facts` limitados, `data_quality` com eventos reais) | `test_unexpected_error_marks_the_opening_run_failed` (`test_opening.py`); `test_failed_cycle_records_its_market_now_and_releases_the_lock` (`test_cycle.py`); `test_outside_a_session_there_is_no_session_open_and_no_staleness`, `test_half_day_session_is_detected_until_its_early_close`, `test_review_flag_without_reasons_is_counted_as_unspecified` (`tests/integration/api/test_health_hardening.py`); `test_per_order_maps_and_feed_lists_in_facts_are_capped_but_causes_see_everything` (`tests/readmodels/test_health_facts.py`); `test_order_detail_shows_real_data_quality_and_data_gap_events` (`tests/integration/api/test_order_quality_api.py`) | OK |
| 13 | Fronteiras: só `bootstrap.py` importa adapters (inclusive `notify/n8n.py`); worker sem adapters/HTTP; módulos puros sem I/O | `test_worker_never_imports_adapters_provider_libraries_or_the_http_layer`, `test_only_the_worker_entrypoint_imports_the_composition_root`, `test_platform_pure_modules_have_no_io_or_infrastructure_imports`, `test_boundary_scan_covers_the_worker_and_alert_packages` (`tests/test_import_boundaries.py`) | OK |
| 14 | `src/core` intocado; `N_BASE` testes herdados verdes; migrations verificadas do zero; ruff e mypy limpos | `git diff`/`git status --porcelain -- src/core` (Step 2, vazios); suíte completa de 908 (inclui os 671 herdados); `test_migrations_run_from_zero_and_back` (`tests/integration/test_schema.py`), `test_downgrade_to_0002_and_back` (`tests/integration/test_schema_0003.py`); `uv run ruff check src tests migrations` e `uv run mypy` limpos | OK |
| 15 | Commits com identidade noreply e sem trailers de IA; nenhuma tag com revisão aberta; nenhum push | `git log --format='%an <%ae>' plan-3a-api..HEAD` (um único autor, `Ricardo Carneiro <132141856+ricdomingues@users.noreply.github.com>`); `git log --format='%B' plan-3a-api..HEAD \| grep -E '^(Co-Authored-By\|Claude-Session):'` (nenhum resultado, Step 2) | OK |

Nenhum nome de teste do brief precisou de correção: os nomes citados na tabela "Critérios de aceite do Plano 3B" (linhas 165–179) apontam para arquivos e áreas, sem nomes de função soltos; os nomes de função acima foram obtidos por grep direto nos arquivos correspondentes e batem com o comportamento descrito em cada critério.

## Rastreio das entradas 1–14 do 3A

| # | Entrada (nota de encerramento do 3A) | Commit(s) | Teste(s) |
|---|---|---|---|
| 1 | Agendar `run_live_cycle`, `run_opening`, `run_end_of_day` com `Services`, `Services.clock` só para `market_now`/`now` | `24db384` (worker/schedule.py, worker/jobs.py) | `tests/worker/test_schedule.py`, `tests/integration/worker/test_jobs.py` |
| 2 | `require_aware` em `evaluate_order(market_now=)`, `finalize_validity(now=)`, `expire_due_orders(now=)` | `3b2b297` | `tests/integration/test_naive_clocks.py` |
| 3 | Webhook `N8N_WEBHOOK_URL` com resumo de fim de dia e alerta quando `/health` ficar `DEGRADED`/`UNHEALTHY` | `e52ba2f` (sink n8n), `e636ec7` (alertas de transição + resumo de fim de dia) | `tests/notify/test_n8n_webhook.py`, `tests/integration/test_health_watch.py` |
| 4 | Alertas do responsável: eventos de ordem, preço cruzando níveis do usuário em tickers da watchlist, limiar de "pressão forte" rotulado como estimativa | `7a7acfc` (outbox), `62f9d0c` (regras da watchlist), `cbaeebe` (estimativas de pressão) | `tests/integration/test_alert_outbox.py`, `tests/integration/test_watchlist_alerts.py`, `tests/alerts/test_rules.py`, `tests/analytics/test_pressure.py` |
| 5 | `DATA_QUALITY_RECHECK` (entrada 16 do Plano 2) consumindo `QUALITY_NOT_EVALUATED` | `fab6b40` | `tests/integration/test_quality_recheck.py` |
| 6 | Spec 6 "yfinance/FMP indisponíveis → `NEEDS_REVIEW`": `DIVIDEND_UNVERIFIED` quando as duas fontes levantam | `f9eacd1` | `tests/integration/test_dividend_unverifiable.py` |
| 7 | Comando `rebuild-projections` | `24db384` (CLI), `e615787` (isolamento por ordem, M5) | `tests/integration/worker/test_cli.py`, `tests/integration/test_rebuild_hardening.py` |
| 8 | M4 — separar erro de conexão de timeout de statement em `/health` | `dc3f9d3` | `test_slow_health_query_is_degraded_not_database_unavailable` |
| 9 | M5 — quarentena não deve congelar ordem replay em `rebuild-projections` | `e615787` | `test_tampered_replay_projection_records_an_incident_without_freezing_the_replay` |
| 10 | M6 — overrides do RECALCULATE validados contra a configuração da ordem de origem | `8e5d298` | `tests/integration/test_recalculate_overrides.py` |
| 11 | M7 — `ALPACA_TRADING_URL` errado (404 sem corpo Alpaca) não pode virar 422 `UNKNOWN_ASSET` | `60c1af3` | `tests/marketdata/test_alpaca_assets_404.py` |
| 12 | `bootstrap.py` fecha o cliente HTTP próprio se `make_engine` levantar | `e52ba2f` | `test_owned_client_is_closed_when_the_engine_cannot_be_built` |
| 13 | Testes faltando: ramo `FAILED` da abertura; detecção de sessão em `/health`; `market_now` em `/health` para runs `FAILED`; `needs_review` com motivos vazios; tamanho do payload `facts` | `3b2b297`, `dc3f9d3` | `test_unexpected_error_marks_the_opening_run_failed`, `test_failed_cycle_records_its_market_now_and_releases_the_lock`, `test_outside_a_session_there_is_no_session_open_and_no_staleness`, `test_half_day_session_is_detected_until_its_early_close`, `test_review_flag_without_reasons_is_counted_as_unspecified`, `test_per_order_maps_and_feed_lists_in_facts_are_capped_but_causes_see_everything` |
| 14 | `data_quality` do detalhe da ordem com eventos reais `DATA_QUALITY`/`DATA_GAP` | `fab6b40` | `test_order_detail_shows_real_data_quality_and_data_gap_events` |

## Decisões D20–D39

- **D20** (composição e agenda do worker: processo único, APScheduler 3.x `BlockingScheduler` ET, jobs `live_cycle`/`watchlist`/`opening`/`end_of_day`/`health_watch`/`deliver_alerts`/`worker_lock`, lock de sessão por banco, desligamento gracioso) — mantida, implementada tal qual na Task 16 — custo se estivesse errada: dois workers concorrentes escrevendo no mesmo pregão, ou jobs se sobrepondo.
- **D21** (`Services.clock()` lido uma vez por job, sempre por `require_aware`; runs `FAILED` do ciclo passam a gravar `market_now`) — mantida, Tasks 2 e 16 — custo se estivesse errada: `/health` e o worker divergiriam sobre o instante de mercado numa falha.
- **D22** (`DATA_QUALITY_RECHECK` fora de `order_events`, linha própria em `data_quality_rechecks`, uma por ordem/pregão, nunca escreve `DATA_QUALITY`/`DATA_GAP`, níveis derivados do ledger até o fechamento do pregão rechecado ou checagem pulada com motivo) — mantida, Task 10 — custo se estivesse errada: reavaliação silenciosa fora do `fill_model v1` congelado, ou toque de nível calculado com níveis errados.
- **D23** (dividendo não verificável: `DIVIDEND_UNVERIFIED`/`DIVIDEND_POSITION_UNCONFIRMED` quando as duas fontes levantam, chave igual à de `apply_dividend(validated=False)`) — mantida, Task 3 — custo se estivesse errada: ordens com dividendo pendente entrariam nas métricas sem revisão.
- **D24** (outbox append-only, `alert_key UNIQUE`, entrega pelo menos uma vez com `Idempotency-Key`, backoff por alerta, orçamento de 20 s por execução, expiração em 24 h, enfileiramento de eventos separado do ciclo live) — mantida, Tasks 7 e 12 — custo se estivesse errada: alertas duplicados no n8n ou um alerta lento bloqueando os demais.
- **D25** (alerta de `/health` só na transição de assinatura estado+causas, com caminho direto pelo sink quando o banco está inacessível) — mantida, Task 14 — custo se estivesse errada: spam de alerta a cada ciclo `DEGRADED`, ou nenhum alerta quando o banco cai.
- **D26** (watchlist e `alert_rules` mutáveis, ingestão em job próprio sem fallback, tickers normalizados, rotas novas no envelope do 3A) — mantida, Tasks 8, 13, 15 — custo se estivesse errada: a watchlist atrasaria a avaliação de ordens, ou um ticker mal normalizado furaria a checagem de negociável.
- **D27** (estimativas de pressão `OHLCV_PRESSURE_ESTIMATE_V1`, módulo puro, `estimate: true` + `method` + `DISCLAIMER` sempre) — mantida, Task 11 — custo se estivesse errada: um alerta de pressão passaria por sinal real de negociação, não uma estimativa.
- **D28** (`/health` distingue timeout de statement de banco indisponível via `statement_timeout` de 5 s e `sqlstate 57014`) — mantida, Task 9 — custo se estivesse errada: uma consulta lenta derrubaria `/health` com 503 em vez de `DEGRADED`.
- **D29** (M5 quarentena nunca congela replay; M6 overrides validados contra a origem; M7 404 sem corpo Alpaca vira `SourceUnavailable`) — mantida, Tasks 4, 5, 6 — custo se estivesse errada: ver entradas 9–11 da tabela de rastreio acima.
- **D30** (webhook composto na raiz, `Services.alert_sink` opcional, `repr` esconde a URL) — mantida, Task 7 — custo se estivesse errada: a URL do webhook vazaria em logs ou em `repr(services)`.
- **D31** (`facts` limitados a 100 entradas por mapa com `_total` quando truncado; `needs_review` sem motivo conta como `UNSPECIFIED`) — mantida, Task 9 — custo se estivesse errada: `/health` cresceria sem limite num banco grande, ou uma ordem corrompida sumiria da fila de revisão.
- **D32** (alerta de recuperação `HEALTH:{log_id}` com `recovered: true` ao voltar a `HEALTHY`, nunca no primeiro estado observado) — mantida, Task 14 — custo se estivesse errada: o responsável nunca saberia que um incidente terminou.
- **D33** (`NEEDS_REVIEW` uma vez por (ordem, motivo, pregão), chave `ORDER_REVIEW:{order_id}:{reason}:{session_day}`) — mantida, Task 12 — custo se estivesse errada: 35 `MISSING_BAR_UNVERIFIABLE` do mesmo pregão virariam 35 alertas.
- **D34** (flags do recheck em ordens vivas: o recheck marca revisão também em ordens abertas ou fechadas; pela política da spec 5.4 uma ordem com `needs_review` sai das métricas padrão, então uma flag de recheck pode retirar retroativamente uma ordem das métricas já exibidas) — mantida, Task 10; **registrado aqui como pedido pelo brief**: esse comportamento é conservador e intencional, não um defeito — custo se estivesse errada: métricas exibidas anteriormente mudariam de leitura sem aviso explícito no dashboard (entrada 3C acompanha isso).
- **D35** (lookback de 4 dias no enfileiramento de eventos; causa `INFO` `ORDER_EVENT_ALERTS_BEHIND` torna a perda visível) — mantida, Task 12 — custo se estivesse errada: alertas de eventos antigos se perderiam sem nenhum sinal em `/health` (mitigado, mas sem corte de idade — ver 3C input 11/M1).
- **D36** (`EVAL_INTERVAL_MINUTES` em 1..59, `CONFIG_INVALID` antes de montar serviços) — mantida, Task 1/16 — custo se estivesse errada: um intervalo fora do campo de minuto do cron travaria a montagem com um traceback em vez de uma mensagem clara.
- **D37** (semântica exata do alerta de pressão forte: janela, CMF/OBV/VWAP quantizados, `BUY`/`SELL` só nas condições exatas, um alerta por último candle) — mantida, Task 11 — custo se estivesse errada: alertas de pressão inconsistentes com a estimativa exibida no `/health`/dashboard.
- **D38** (runs de abertura/fim de dia ausentes viram causas `DEGRADED` em `/health`, ancoradas no calendário NYSE, dentro de `MISSING_RUN_LOOKBACK` de 14 dias) — **corrigida na revisão final**: a âncora original (`close_utc + END_OF_DAY_GRACE` de 3h) gerava falso `END_OF_DAY_MISSING` em todo meio-pregão NYSE (I1); a âncora passou a ser `max(close_utc + 3h, 19:00 ET)`, cobrindo de fato o retry das 18:30 — custo de ter ficado errada: alertas `DEGRADED`/recuperação falsos cerca de três vezes por ano (3 de julho, dia depois de Ação de Graças, véspera de Natal), com webhook acionado sem incidente real.
- **D39** (lock do worker vigiado a cada 1 min via `pg_locks`; ao perdê-lo, alerta `WORKER_LOCK_LOST` direto pelo sink, `scheduler.shutdown(wait=False)`, saída com código 3) — mantida, Task 16, com um endurecimento na rodada final: um segundo `SIGTERM`/`SIGINT` (ou um sinal chegando depois da perda do lock) agora é idempotente via uma flag `stopping`, em vez de chamar `shutdown()` duas vezes e derrubar `finally`/`services.close()` sob jobs ainda em andamento — custo se estivesse errada (antes do endurecimento): um segundo sinal encerraria o processo de forma não graciosa; o próprio `wait=False` do caminho de perda de lock continua fechando serviços com jobs em andamento por desenho (D39), registrado como entrada 3C 19 junto com a desigualdade de cadência `*/N` quando `N` não divide 60.

## Rulings durante a execução

1. **Task 0 não despachada.** `N_BASE = 671` passando, 0 skips, 0 warnings, medido do zero em `9cb2889` (a verificação final do Plano 3A); `8cfb743` e `57d3d8d` diferem só em documentação, então a base efetiva do Plano 3B é a mesma árvore. Custo se estivesse errada: a linha de base da Task 1 ficaria errada, mas a própria corrida completa da Task 1 pegaria a discrepância.
2. **Resumo do pytest oculto por `-q`.** Os passos do plano que usavam `uv run pytest -q | tail -1` foram trocados por `uv run pytest` puro, porque `pyproject.toml` já tem `-q` em `addopts` e o `tail -1` escondia o resumo. Custo se estivesse errada: nenhum.
3. **Arquivo de re-revisão perdido em `scratchpad/`.** Um arquivo de re-revisão gravado por engano sob o `scratchpad/` do repositório foi movido para o scratchpad da sessão antes do commit do plano. Custo se estivesse errada: nenhum.
4. **Composição da rodada de correção final.** A rodada única cobriu I1 + I2 (helper de envelope de alerta compartilhado, que também fecha o achado "chaves reservadas" da Task 12) + I3 (que fecha o achado adiado da Task 5) + `require_aware(now)` na Task 12 + a flag `stopping` de sinal duplo da Task 16 (com asserção de `wait=True`) + os testes de DST da Task 16 + `require_aware` em `_alert_lock_lost` da Task 16 — nenhum item toca `src/core` nem os escritores do Plano 2. Custo se estivesse errada: menos texto de diagnóstico nos corpos 422.
5. **Destino dos achados menores.** M1–M4 (da revisão final) e os achados menores adiados pelos revisores de task ficam registrados nesta nota como entradas para o Plano 3C; os achados descartados (drop) ficam marcados como descartados, não adiados. Custo se estivesse errada: o Plano 3C precisaria tratá-los antes do Compose de produção.

## Resultado da revisão final

Revisão de branch inteiro (opus, `8cfb743..2f57ed5`): **Com correções** — 0 Critical, 3 Important, 4 Minor.

- **I1 — falso `END_OF_DAY_MISSING` em todo meio-pregão NYSE**, que também disparava um alerta `DEGRADED` e um alerta de recuperação pelo webhook. Corrigido em `138e4f1`: a âncora do D38 passou de `close_utc + 3h` para `max(close_utc + 3h, 19:00 ET)`. Teste: `test_half_day_end_of_day_deadline_follows_the_1830_retry_not_the_close`.
- **I2 — alertas diretos de `/health` enviados com o banco fora do ar não carregavam o envelope da D24** (`schema_version`/`alert_key`/`kind`), o que faria um roteamento do n8n por essas chaves descartar exatamente o alerta mais importante (banco inacessível). Corrigido em `1b55102`: helper `alert_envelope()` compartilhado, mesclando as chaves reservadas por último, usado pelo outbox, pelo alerta direto de `/health` e por `WORKER_LOCK_LOST`.
- **I3 — texto de exceção/decodificador ainda chegando a corpos HTTP** (`REPLAY_REQUEST_INVALID` ecoava `str(exc)`; `INVALID_JSON` ecoava a mensagem do decodificador JSON), contra a Global Constraint "nenhum texto de exceção em respostas HTTP". Corrigido em `8da0f24`: `ReplaySelectionError` passou a carregar `codes` fixos; `read_json_object` passou a reportar só `{line, column}`/`{}`/`NOT_AN_OBJECT`.

Junto com I1–I3, a mesma rodada corrigiu quatro achados menores já marcados "fix-before-merge" nas revisões de task: `deliver_pending_alerts` sem `require_aware(now)` (Task 12), um segundo `SIGTERM`/`SIGINT` derrubando o scheduler de forma não graciosa (Task 16), ausência de testes de transição de horário de verão para `next_fire`/`live_session` (Task 16) e `_alert_lock_lost` sem `require_aware` (Task 16).

Rodada de correção: commits `138e4f1` (I1), `1b55102` (I2 + `require_aware` da Task 12), `31eef0e` (os três itens da Task 16), `8da0f24` (I3, que também fecha o achado adiado da Task 5). Resultado: 908 passando, 0 skips, 0 warnings (899 + 9 testes novos). Re-revisão de escopo restrito (sonnet): os 7 achados foram todos marcados ADDRESSED, sem quebra nova — com uma observação registrada para o 3C (não um achado bloqueante): a flag `stopping` é compartilhada entre a thread que vigia o lock e o manipulador de sinal por melhor esforço (GIL), sem sincronização explícita.

## Achados menores adiados

**Descartados (drop) pela triagem da revisão final** — fechados sem ação, não são entradas do 3C:
- Task 2 — teste de `expire_due_orders` com relógio naive não força falha de banco (assimétrico com o teste de `finalize_validity`): a guarda é a primeira instrução da função, a assimetria é cosmética.
- Task 3 — checagem redundante final/frozen em `corporate.py:117`: defesa inofensiva dentro da transação já travada.
- Task 7 — `json.dumps(allow_nan=False)` fora do `try` em `n8n.py`: `deliver_pending_alerts` já captura `Exception` e grava `FAILED`/`ValueError`; os documentos passam por `to_document` (strings decimais).
- Task 10 — o `run` `FAILED` do recheck grava `repr(exc)` no detalhe, como todo outro escritor; `/health` já sanitiza para o tipo (D19).
- Task 14 — `HealthWatcher.observe` (leitura-depois-inserção) depende do worker único: garantido por D20/D39 e `max_instances=1`.

**Adiados para o Plano 3C** — viram as entradas 11–20 listadas abaixo: M1 (`ORDER_EVENT_ALERTS_BEHIND` sem idade máxima), M2 (keepalive da conexão do lock), M3 (janela da watchlist duplicada sem teste de igualdade), M4 (traceback bruto na subida do worker sem banco), o `_JOB_RUNS` sem limite da Task 9, a ausência de `UNIQUE(order_id, session_date)` e as duas lacunas de teste do recheck da Task 10, a lista branca de payload e as varreduras sem limite do outbox da Task 12, o aborto do ciclo inteiro da watchlist por uma exceção inesperada de ingestão da Task 13, e o `shutdown(wait=False)`/desigualdade de cadência `*/N` da Task 16. A pendência `PROVIDER_FAILURE` que sai de `/health` depois de 20 runs `END_OF_DAY` (Task 10) já era a entrada 9 do brief e continua listada lá.

## Entradas para o Plano 3C

1. Docker Compose de produção com os serviços `api`, `worker` (`python -m virtual_orders.worker`), `dashboard` e `postgres`:
   - um único `worker` (D20), com `restart` ativo (a D39 sai com código 3 ao perder o lock) e `stop_grace_period` de pelo menos 120 s, para um fim de dia ou recheck em andamento terminar no `SIGTERM`;
   - healthcheck do `api` (com a chave ou um `/livez` sem banco, decisão do 3C).
2. Rotas de candles para a UI (as-of, com volume e VWAP) e sobreposição de ordens; exibição das estimativas de pressão sempre com `method` e `DISCLAIMER` (D27, D37).
3. Telas da watchlist e das regras sobre as rotas da Task 15.
4. Dashboard exibindo as causas `INFO` `UNDELIVERABLE_ALERTS` e `ORDER_EVENT_ALERTS_BEHIND`, os alertas `EXPIRED` e o último `health_state_log`.
5. Recuperação de jobs perdidos (D38): comando do CLI para rodar a abertura ou o fim de dia de um pregão passado, com as travas da D4/D12, ou decisão explícita de manter a recuperação manual.
6. Rever, depois da observação em paper, se o colapso de revisões por (ordem, motivo, pregão) da D33 é suficiente ou se precisa de um resumo por ordem.
7. Portfólio virtual; portfólio real da Robinhood somente leitura, condicionado à Fase 0 do roadmap.
8. Dashboard exibindo `data_quality.rechecks` do detalhe da ordem (incluindo `status`, `terminal_reason` e `level_touch_check`).
9. Pendências `PROVIDER_FAILURE` que persistem além dos 20 últimos runs `END_OF_DAY` saem de `/health` sem linha terminal; decidir se ganham um status terminal próprio.
10. Burst de cruzamentos ao ligar o webhook no meio do pregão (D26): rever com dados reais de paper se precisa de um corte de frescor.
11. **M1** — `ORDER_EVENT_ALERTS_BEHIND` não tem idade máxima e varre todos os eventos de ordem; limitar por uma janela (ex.: `recorded_at >= clock_timestamp() - UNDELIVERABLE_WINDOW`) e a mesma varredura roda hoje dentro do orçamento de 5 s de `/health`.
12. **M2** — a conexão do lock do worker não tem keepalive nem timeout de socket; uma sessão TCP meio-aberta pode travar `conn.execute` indefinidamente e, com `max_instances=1`, o job `worker_lock` para de rodar sem a D39 disparar. Configurar `keepalives`/`tcp_user_timeout` do libpq em `make_engine` no Compose de produção do 3C.
13. **M3** — a janela de pregão vivo (`watch.py` `WATCH_GRACE`/`watch_session`) duplica `worker/schedule.py` (`LIVE_WINDOW_GRACE`/`live_session`) sem um teste que fixe a igualdade; a correção do I1 também duplicou `_MARKET_TZ`/`END_OF_DAY_FLOOR` em `health.py` pelo mesmo motivo (módulos neutros não podem importar a camada de aplicação). Adicionar um teste de igualdade, ou extrair as constantes para um módulo neutro compartilhado.
14. **M4** — o worker subindo com o banco fora do ar (`acquire_worker_lock` levantando) sai com um traceback não tratado (código de saída 1) em vez de uma linha logada; `finally` ainda fecha os serviços e o supervisor reinicia o processo, mas vale logar `type(exc).__name__` e devolver um código distinto.
15. **T9** — `_JOB_RUNS` (`readmodels/health.py`) busca todos os runs `OPENING`/`END_OF_DAY` sem limite; usar `MISSING_RUN_LOOKBACK` e uma consulta separada de `min(session_day)`.
16. **T10** — não existe `UNIQUE(order_id, session_date)` no banco para rechecks (a garantia depende do lock da linha da ordem em `apply_command` + `_already_rechecked`, correto hoje mas sem reforço de schema); acrescentar como migration 0004. Duas lacunas de teste continuam abertas: o ramo de corrida `ALREADY_EVALUATED` não é exercitado, e o teste de toque de nível no nível de integração não distingue níveis derivados de níveis de projeção.
17. **T12** — o payload/`first_ref` do outbox é copiado sem lista branca (os payloads do núcleo já são estruturados hoje); `_CANDIDATES`/`_BEHIND` fazem varreduras sem limite; não há trava própria contra entregadores concorrentes (depende da semântica "pelo menos uma vez" de D20/D24); e há lacunas de teste (incidente de replay alertado, falha-depois-expiração, limites, gerenciador de contexto do cliente).
18. **T13** — uma exceção inesperada de ingestão aborta o ciclo inteiro da watchlist antes mesmo do run começar (hoje contida pelo runner, com retry no próximo ciclo); considerar capturar por ticker em `_ingest` como `ERROR:<Type>`. `run` `FAILED` da watchlist também grava `repr(exc)` como o ciclo live. Lacunas de teste: falha parcial multi-ticker, `UNKNOWN_DATA_SOURCE`, bordas de `watch_session`, pressão `SELL`, corte de criação de pressão.
19. **T16** — a perda do lock usa `shutdown(wait=False)` e fecha os serviços com jobs ainda em andamento (mandado pelo plano, D39, registrado nesta nota); o ramo "vivo mas com lock perdido" de `worker_lock_held` não é testado; a cadência `*/N` fica desigual quando `N` não divide 60 (nota já na D36).
20. Um erro de escopo do `conftest` do pytest ocorre ao passar arquivos de teste da API junto com arquivos de fora da API como argumentos soltos na mesma chamada (fixture `api` não encontrada); reproduzido na revisão final e nas Tasks 10/16; os mesmos arquivos passam isoladamente. Sem impacto em produção — é só uma questão de como a suíte é invocada.

**Escopo do responsável para 3C**, restabelecido em 2026-09-13: candles com volume/VWAP e sobreposição de ordens; exibição de pressão sempre rotulada como estimativa; telas de watchlist e regras; portfólio virtual + portfólio real da Robinhood somente leitura, este último condicionado à Fase 0 do roadmap.
