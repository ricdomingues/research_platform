# Plan 2 (Virtual Order Engine — Persistência, Ledger, Dados, Evaluator, Replay) — Encerramento

- **Plano:** `docs/superpowers/plans/2026-09-12-virtual-order-engine-persistence.md`
- **Spec:** v1.2 (`spec/virtual-order-engine-v1.2`) + decisões D5–D11 registradas no plano
- **Intervalo:** `13757ab..e83b88f` (Tasks 1–16, incluindo rodadas de correção; esta nota e o teste ponta a ponta da Task 17 são commitados em seguida, sobre esse intervalo)
- **Testes (remedidos após a onda de correção final):** 426 passando = 180 herdados do Plano 1 (`tests/domain`, `tests/fills`, `tests/marketdata/test_nyse_calendar.py`, `tests/metrics`, `tests/test_actionability.py`, `tests/test_dataquality.py`) + 246 do Plano 2 (98 unitários — `tests/marketdata/test_asof_hash.py`, `test_http_sources.py`, `test_yfinance_source.py` (20), `tests/storage` (8), `tests/test_hypothesis_hermetic.py` (4), `tests/test_import_boundaries.py` (66) — e 148 de integração contra Postgres real); duração da suíte completa 55,94 s (`uv run pytest -p no:cacheprovider -o addopts="" -q`); nenhum skip. A versão anterior desta nota dizia "200 herdados do Plano 1", contando por engano os 20 testes do Plano 2 em `tests/marketdata`
- **Núcleo congelado:** `git diff --stat plan/virtual-order-engine-core-complete -- src/core` e `git status --porcelain -- src/core` vazios — confirmado em `e83b88f` (fim da Task 16) e reconfirmado nos comandos do Step 4 executados imediatamente antes deste commit

## Critérios de aceite adicionais
| Critério | Teste | Resultado |
|---|---|---|
| nenhuma dependência de I/O entrou em `src/core` | `test_frozen_core_never_imports_platform_or_io` + diff vazio | OK |
| adapters substituíveis | suíte com `fake_feed` + `test_recorded_session_end_to_end` com Alpaca | OK |
| payloads de provider não vazam | `test_neutral_infrastructure_never_imports_provider_adapters` | OK |
| proveniência não hard-coded | `test_provenance_accepts_any_provider_without_migration` | OK |
| as-of independente do provider | `test_as_of_reads_are_isolated_by_source` | OK |
| `selected_data_hash` identifica os dados usados | `test_hash_changes_with_source_batch_price_and_ticker`, `test_backdated_version_breaks_regeneration_and_freezes` | OK |
| REPRODUCE preserva missing do run original | `test_missing_minute_is_missing_for_reproduce_but_present_for_recalculate` | OK |
| RECALCULATE usa dados posteriores | idem + `test_recalculate_uses_corrected_history_without_touching_the_source` | OK |
| sem troca silenciosa de provider | `test_no_silent_provider_switch` | OK |
| Plano 1 verde | suíte completa (180 testes herdados, todos passando) | OK |

## Decisões confirmadas ou alteradas na execução
(uma linha por decisão D5–D11 e por decisão de detalhe 1–13: mantida / alterada — motivo — custo se estiver errada)

1. **D5** (`data_as_of` sob `INGEST_LOCK_KEY`, pós-ingestão, relógio do banco) — mantida — implementada tal qual nas Tasks 6, 9, 11, 15; nenhum caminho de leitura "current" foi introduzido — custo se estiver errada: reavaliação histórica passaria a depender do relógio da aplicação, quebrando reprodutibilidade.
2. **D6** (ausência imutável no run; REPRODUCE vê `MISSING`, RECALCULATE vê dado corrigido; candle não fechado não é lido nem ausente) — mantida — provada por `test_missing_minute_is_missing_for_reproduce_but_present_for_recalculate` e `test_recalculate_uses_corrected_history_without_touching_the_source` — custo se estiver errada: replays deixariam de ser auditáveis (histórico mutável).
3. **D7** (`CoveragePolicy` injetável, padrão `STRICT_PRIMARY`, 503 `ACTIONABILITY_UNVERIFIABLE`) — mantida, com reforço na revisão da Task 11: `verified=True` com minutos não resolvidos é tratado como violação de contrato e nunca constrói ordem; `actionability_coverage_policy`/`actionability_coverage_evidence` passaram a constar do `ORDER_CREATED` — motivo: uma política que mentisse `verified` deixaria passar ordens com buracos silenciosos — custo se estiver errada: uma política futura de provider secundário poderia reintroduzir esse buraco sem ser pega por teste.
4. **D8** (adapters via `httpx` fora de `src/core`, fixtures no nível HTTP, gateway sem fallback) — mantida — confirmada pela varredura de fronteiras (`test_neutral_infrastructure_never_imports_provider_adapters`) e pelo teste ponta a ponta com `httpx.MockTransport` — custo se estiver errada: acoplaria o núcleo a um provider específico.
5. **D9** (colunas de infraestrutura/auditoria aprovadas, sem alterar modelos de `src/core`) — mantida — nenhuma coluna nova em `src/core`; todas em `virtual_orders/storage/tables.py` — custo se estiver errada: reabriria o núcleo congelado.
6. **D10** (sem `DATA_QUALITY_RECHECK` neste plano) — mantida — nada foi criado; `RECALCULATE` é o único caminho de reavaliação — custo se estiver errada: reavaliações de `DATA_QUALITY` ficariam sem trilha própria até um plano futuro.
7. **D11** (fixtures synthetic/documentation-derived, catalogadas em `tests/fixtures/README.md`) — mantida — inclusive as barras do teste ponta a ponta (`scenario_bars`) são fixtures sintéticas rotuladas — custo se estiver errada: testes poderiam ser confundidos com capturas reais de mercado.
8. **Detalhe 1** (colunas extras: `evaluation_start_ts`, `price_source`, `hash_material`, `state_document`, `next_seq`, `first_seq`/`event_count`, `evaluation_run_status.id`; triggers append-only em `orders`/`signals`) — mantida — todas presentes em `virtual_orders/storage/tables.py` e nas migrations.
9. **Detalhe 2** (`evaluation_runs.kind` ganha `END_OF_DAY`) — mantida — usada por `run_session_quality`/`run_end_of_day`.
10. **Detalhe 3** (relógio de mercado `market_now`/`created_at` separado do relógio de conhecimento `data_as_of`) — mantida, com reforço na revisão da Task 14: `run_session_quality` passou a exigir `market_now` e a só aceitar o pregão que acabou de fechar (D4) — custo se estiver errada: `DATA_QUALITY` poderia ser emitida para um pregão ainda aberto ou muito antigo.
11. **Detalhe 4** (ingestão deduplicada por OHLCV da mesma fonte) — mantida — achado menor adiado (Task 6): a comparação ignora mudança de `provider`/`provider_version` mesmo com OHLCV igual; não altera a decisão em si.
12. **Detalhe 5** (`selected_data_hash` = SHA-256 canônico de `(source, ticker, ts, batch_id, OHLCV)` ou `(source, ticker, ts, MISSING)`) — mantida — provada pelos testes de hash listados na tabela acima.
13. **Detalhe 6** (`selected_data_hash` da actionability no `detail` do run e no `ORDER_CREATED`) — mantida, ampliada na Task 11 com `actionability_coverage_policy`/`actionability_coverage_evidence` no mesmo payload.
14. **Detalhe 7** (qualidade de fim de dia: `DATA_GAP` intra-pregão, conferência diária só com janela completa, yfinance indisponível registrado no `detail`, ordens replay sem `DATA_QUALITY` live) — alterada na revisão da Task 14: isolamento por ordem (uma falha de integridade não aborta o restante do lote), `LEFT JOIN` para não pular ordens sem projeção, e o próprio `run_end_of_day` passou a não expirar/qualificar quando o ciclo foi pulado — motivo: spec 3.3/6 (incidentes nunca silenciados) e D4 exigiam essas correções — custo se estiver errada: incidentes de projeção ausente ou pregões incorretos passariam despercebidos.
15. **Detalhe 8** (REPRODUCE regenera histórico com o algoritmo do rebuild, exige identidade `(event_key, payload_hash)`, segmentos apontam para os runs originais, divergência vira `LedgerIntegrityError(REPRODUCE_DIVERGENCE)` com `frozen`) — mantida, com isolamento por fonte acrescentado na revisão da Task 15 (`OrderNotFound`/`LedgerIntegrityError`/qualquer outra exceção viram falha daquela fonte; o run `REPLAY` sempre termina) — custo se estiver errada: um erro de programação inesperado abortaria o lote inteiro em vez de aparecer no relatório.
16. **Detalhe 9** (RECALCULATE usa o `price_source` de origem, lê as-of o `data_as_of` do snapshot, intercala dividendos por trecho, aplica validade se `data_as_of ≥ valid_until_ts`, não copia `CANCELED`/`FROZEN`/`NEEDS_REVIEW`) — alterada na revisão da Task 16: passou a rejeitar `data_as_of` *naive* ou posterior ao watermark de ingestão antes de abrir o run — motivo: D5/spec 3.6 exigem que a seleção de dados fique fixa e auditável, nunca dependente de dados que chegarão depois da chamada — custo se estiver errada: quem quiser "agora" precisa passar `None` em vez de um instante futuro explícito.
17. **Detalhe 10** (dividendos: job de abertura exige `now` anterior à abertura do pregão da ex-date; valor creditado é o da fonte primária validada) — alterada na revisão da Task 13: conjunto de candidatos restrito a ordens não finais/não congeladas/não replay com posição confirmada até o fechamento do pregão anterior (senão `NEEDS_REVIEW:DIVIDEND_POSITION_UNCONFIRMED`); o crédito passou a usar exatamente a linha já gravada em `dividends` (primeira checagem vale para registro e crédito); divergência de tolerância vira `NEEDS_REVIEW:DIVIDEND_TOLERANCE_MISMATCH` sem crédito — motivo: a idempotência estrita da spec 4.7 exigia que registro e crédito nunca se contradissessem — custo se estiver errada: posições não avaliadas ou com tolerância divergente da global exigem revisão manual em vez de crédito automático.
18. **Detalhe 11** (contratos de dados síncronos, "conceitualmente equivalente a `async def get_bars`" atendido pela fronteira, não pela assincronia) — mantida — todo o Plano 2 usa SQLAlchemy síncrono; nenhum `FundamentalsProvider` foi criado por falta de consumidor.
19. **Detalhe 12** (Plano 2 vive em `virtual_orders/`, não em `core/` como a spec 2.2 lista; responsabilidades da tabela 2.2 preservadas) — mantida — ruling do pré-flight (congelamento + D8 são mais recentes e explícitos que a spec) — custo se estiver errada: renomear o pacote em todo o Plano 2.
20. **Detalhe 13** (baseline de lint/tipos do Plano 1 silenciada por `per-file-ignores`/overrides de mypy, nunca corrigida) — mantida — confirmado em `pyproject.toml` (`[tool.ruff.lint.per-file-ignores]` e `[[tool.mypy.overrides]]` para `core.fills.v1.engine`).

## Rulings durante a execução
(formato das notas do Plano 1: decisão — motivo — custo se estiver errada)

1. Plano 2 vive em `src/virtual_orders/`, não em `core/` como a spec 2.2 lista — o congelamento do responsável ("`src/core` não deve ser alterado") e a D8 ("adapters fora do core") são mais novos e explícitos; as responsabilidades da tabela 2.2 são preservadas — custo se estiver errada: renomear o pacote em todo o Plano 2.
2. Os contratos de dados permanecem síncronos (o responsável pediu apenas "conceitualmente equivalente a `async def get_bars`"); todo o Plano 2 é SQLAlchemy síncrono — custo se estiver errada: wrappers assíncronos depois, na fronteira do adapter.
3. D5–D11 registradas só no plano; a spec v1.2 permanece congelada — custo se estiver errada: um documento de emenda v1.3 mais tarde.
4. Commits de subagentes levam o próprio modelo no `Co-Authored-By` (ex.: Claude Sonnet 5) em vez do trailer Opus do plano; a linha `Claude-Session` é mantida — a atribuição deve nomear o modelo que de fato escreveu o commit — custo se estiver errada: reescrita cosmética do trailer antes do merge.
5. Corrigido o desenho de quarentena mandado pelo plano (Task 7) — a spec 3.3 ("incidentes nunca são silenciados") prevalece sobre o código do plano; o incidente é gravado em sua própria transação (order_id preservado só se a ordem existir, senão NULL com o id no detail), o congelamento numa segunda transação cuja falha nunca substitui o `LedgerIntegrityError` original; a API pública permanece igual para as tasks seguintes — custo se estiver errada: uma transação extra por incidente.
6. Corrigido via novo arquivo `tests/conftest.py` (Plano 2) que neutraliza o pool de constantes locais do Hypothesis para toda a sessão (falha alto se o hook privado desaparecer) mais um teste de regressão; nenhuma edição em `tests/fills` nem em `src/core` — mantém os artefatos do Plano 1 congelados e torna todo teste de propriedade derandomizado hermético contra literais futuros em `virtual_orders`/core — custo se estiver errada: acoplamento a uma API privada do Hypothesis (quebra alto em upgrade); a alternativa seria editar o módulo de teste do Plano 1.
7. Corrigida a query mandada pelo plano (Task 9) — a spec 3.3/6 (incidentes nunca silenciados) prevalece; `LEFT JOIN order_state` passa a incluir ordens sem projeção (não-replay) para que `evaluate_order` levante `PROJECTION_MISSING` e a quarentena registre o incidente; teste de ciclo completo acrescentado — custo se estiver errada: uma ordem sem projeção geraria um incidente por ciclo até ser reconstruída (ruidoso, mas visível).
8. Aceito o desvio de mecanismo do Fix F1 — esvaziar totalmente o pool de constantes locais quebrava o próprio teste do Plano 1 (sua linha de base já inclui literais de `src/core`), então a varredura foi restrita a `src/core` apenas; isso reproduz exatamente a linha de base congelada do Plano 1 e exclui `virtual_orders` e qualquer arquivo futuro fora de `src/core` — custo se estiver errada: o teste de frequência do Plano 1 continua acoplado aos literais de `src/core` (congelado, logo estável) e a dois internals privados do Hypothesis (guardados por `pytest.exit` alto).
9. Corrigidos os três achados da Task 11 contra a spec 3.4.1/D7 — `verified` precisa implicar ausência de minutos não resolvidos (senão `ACTIONABILITY_UNVERIFIABLE` com `policy_contract_violation` e nunca constrói ordem; `ORDER_CREATED` também grava `actionability_coverage_evidence`); T = `created_at` ou `now` capturado uma única vez antes da ingestão e usado tanto para o limite de ingestão quanto para a decisão; a expiração (`T >= valid_until_ts`) é checada antes de qualquer ingestão, registrada como um run `ACTIONABILITY` `COMPLETED` com `SIGNAL_EXPIRED` — custo se estiver errada: uma futura política de provider secundário também precisará fornecer barras verificadas (já previsto no roadmap Robinhood).
10. Aceito "incidente + projeção ausente" como equivalente a congelado para uma projeção `DELETED` divergente durante o rebuild (Task 12) — gravar uma projeção ou evento `FROZEN` a partir de um histórico divergente fabricaria estado; a ordem já falha fechada (ciclo/comandos levantam `PROJECTION_MISSING` e a quarentena age a cada toque) — testes negativos de regeneração acrescentados para tornar as comparações da D2 não vazias — custo se estiver errada: o `/health` do Plano 3 precisa tratar ordens com incidente `PROJECTION_INTEGRITY_ERROR` e sem projeção como congeladas.
11. Corrigido o desenho de dividendos da Task 13 contra a spec 4.7 e a idempotência estrita — conjunto de candidatos = ordens não finais/não congeladas/não replay de um ticker com registro de dividendo; uma ordem cujo último segmento avaliado termina antes do fechamento do pregão anterior recebe `NEEDS_REVIEW:DIVIDEND_POSITION_UNCONFIRMED:{ex_date}` sem crédito; `now` precisa estar entre o fechamento do pregão anterior e a abertura do pregão da ex-date (senão `ValueError`); depois do insert, o valor e a validação vêm da própria linha gravada em `dividends` (primeira checagem vale para registro e crédito); tolerância divergente da configuração da ordem vira `NEEDS_REVIEW:DIVIDEND_TOLERANCE_MISMATCH:{ex_date}` sem crédito — custo se estiver errada: dividendos de posições obsoletas/não avaliadas exigem revisão manual; os dados de teste de corporate actions existentes precisaram ser ajustados para posições avaliadas até o fechamento anterior.
12. Corrigidos os quatro achados da Task 14 contra a spec 3.3/6 e D4 — isolamento por ordem como no `run_live_cycle` (um `LedgerIntegrityError` vira `OrderOutcome.error` e entra no detail do run; qualquer outra exceção finaliza o run `END_OF_DAY` como `FAILED` antes de relançar); `LEFT JOIN order_state` para que ordens não-replay sem projeção apareçam como incidentes `PROJECTION_MISSING`; `run_end_of_day` deixa de expirar ordens e gravar qualidade quando o ciclo foi pulado (`EndOfDayReport.quality` vira `Optional`, `expired=()`); `run_session_quality` ganha `market_now: datetime | None = None` e levanta `ValueError` a menos que `session_day` seja o último pregão cujo fechamento seja `<= market_now` — custo se estiver errada: reprocessar o `DATA_QUALITY` de um pregão perdido exigiria um caminho de `RECHECK` futuro explícito (já adiado pela D10).
13. Isolamento por fonte no `reproduce_orders`/`recalculate_orders` (Task 15, herdado pela Task 16) — qualquer exceção fora de `OrderNotFound`/`LedgerIntegrityError` vira `failures[source_id] = "ERROR:<Type>"` e o lote continua; `finish_run` sempre roda (`COMPLETED` com `created`/`failures`; `FAILED` com `error` só se algo fora desse tratamento por fonte quebrar) — custo se estiver errada: erros de programação inesperados são reportados por fonte em vez de derrubar o processo ruidosamente (continuam visíveis no relatório e no detail do run).
14. `recalculate_orders` adquire o watermark primeiro e levanta `ValueError` antes de abrir o run se `data_as_of` for *naive* ou posterior ao watermark de ingestão (sem clamping — quem pediu um instante de conhecimento específico recebe exatamente isso ou um erro) — D5/spec 3.6 exigem isso — custo se estiver errada: quem quiser "agora" precisa passar `None`.
15. Onda de correção final (revisão do branch inteiro) — isolamento por ordem em `run_live_cycle`, `run_session_quality`, `expire_due_orders`, `apply_dividends` e `freeze_for_splits` (integridade → `error.kind`, qualquer outra exceção → `ERROR:<Type>`; runs terminam `COMPLETED` com `integrity_errors`/`order_errors`, `FAILED` só fora do laço por ordem); `load_projection` converte `state_document` indecifrável em `PROJECTION_INTEGRITY_ERROR`; `run_end_of_day` não expira ordens cujo `price_source:ticker` falhou na ingestão do próprio ciclo; consultas de expiração/dividendos/splits passam a `LEFT JOIN order_state` (ordens sem projeção viram incidentes `PROJECTION_MISSING`); ordens com `evaluation_start_ts >=` abertura da ex-date não são candidatas a dividendo (sem evento, sem sinalizador); índice `evaluation_run_status (run_id, id DESC)` — motivo: uma ordem ruim não pode parar a avaliação das demais nem apagar o `DATA_QUALITY` do pregão (D4 impede refazer), e nenhum resultado final pode nascer sem dados — custo se estiver errada: erros de programação passam a aparecer só no detail do run e no `OrderOutcome` (Plano 3 precisa exibi-los no `/health`).

## Observabilidade a acompanhar em paper
- Frequência de `ACTIONABILITY_UNVERIFIABLE` via `actionability_outcomes` (D7), por ticker (`detail.ticker`) e `missing_count`.

## Entradas para o Plano 3
1. Composição: montar `MarketDataGateway` a partir da configuração (hoje só `AlpacaBars`); `price_source` configurado é passado a `submit_signal` e `create_manual_order`; nenhum outro módulo instancia adapters.
2. `POST /signals` → `submit_signal` (201 CREATED / 200 EXISTING / 409 `IdempotencyConflict` / 422 `SignalValidationError`); verificação de ticker negociável antes do serviço; corpo JSON lido com `parse_float=Decimal`.
3. `POST /signals/{id}/orders` → `create_manual_order` (422/503 por `ManualOrderError.code`).
4. `POST /orders/{id}/cancel` → `cancel_order` (`OrderAlreadyFinal` → 409).
5. `POST /replay` → `reproduce_orders` / `recalculate_orders` (`ReplaySelectionError` → 422).
6. Worker: `run_live_cycle` a cada `EVAL_INTERVAL_MINUTES`; abertura: `apply_dividends` + `freeze_for_splits`; fim de dia: `run_end_of_day`; `rebuild-projections` → `rebuild_all_projections`.
7. `/health`: último run `LIVE` e status, `ingest_failures` consecutivos por `price_source:ticker` (3 ciclos → degradado), `UNKNOWN_DATA_SOURCE`, `integrity_incidents`, fila `NEEDS_REVIEW`, ordens `frozen`, contagem de `ACTIONABILITY_UNVERIFIABLE`.
8. `GET /metrics`: montar `TradeResult` a partir de `order_state` (como em `test_end_to_end.trades`), aplicar filtros `replay`/`needs_review` na API.
9. Rejeitar ordens replay em comandos (`cancel_order`, `freeze_order`, `flag_order_review`) e rejeitar replay de replay em `reproduce_orders`/`recalculate_orders`.
10. Job de abertura grava uma linha de run própria com as fontes consultadas e as falhas (dividendos/splits hoje só retornam `OrderOutcome`).
11. Teto de páginas em `AlpacaBars._pages` (um `next_page_token` repetido hoje causaria loop).
12. Rejeitar `market_now` *naive* em `run_live_cycle`/`run_session_quality`/`run_end_of_day` (hoje interpretado como hora local).
13. Validar `config_overrides` antecipadamente e rejeitar (ou documentar) o `dividend_tolerance` ignorado pelo RECALCULATE.
14. Tornar `recalculate_order` privado (sem guarda própria de timezone/watermark).
15. Exibir no `/health` os resultados `ERROR:*` (`order_errors` do detail dos runs `LIVE`/`END_OF_DAY`) ao lado dos incidentes de integridade.

## Achados menores adiados

**Fix F1 (hermeticidade do Hypothesis) — causa raiz e correção:** Hypothesis 6.168 escaneia o AST de todo módulo local recém-importado e injeta literais numéricos no pool global de "constantes locais" usado por `st.integers`/`st.floats`, mesmo sob `derandomize=True`; o literal `10000` (limite de página) em `virtual_orders/marketdata/alpaca.py` caía dentro das faixas de `tests/fills/test_properties.py`, mudando deterministicamente a frequência de `STOPPED_BREAKEVEN`/`TARGET2_HIT` apenas por causa da ordem de coleta do pytest. Corrigido em `tests/conftest.py` (não em `src/core`, não em `tests/fills`): `hypothesis.internal.conjecture.providers.is_local_module_file` é envolvido para só tratar como "local" (logo, elegível ao scanner) arquivos dentro de `src/core/` — exatamente o conjunto já escaneado quando o Plano 1 foi calibrado e congelado. Qualquer arquivo do Plano 2 (`virtual_orders/**`) ou teste fora de `tests/fills` fica permanentemente fora do pool, imune à ordem de importação. Falha alto (`pytest.exit`) se os internals do Hypothesis mudarem de nome numa atualização futura.

**Nota sobre identidade de autor Git:** os commits deste branch foram gravados como `Ricardo Carneiro <ricardocarneiro@Ricardos-MacBook-Air.local>` (identidade derivada da máquina; `user.email`/`user.name` não configurados no Git local — o e-mail do usuário, `sanches.lms@gmail.com`, não está associado a nenhum commit). Nenhuma alteração de configuração foi feita; sinalizado ao responsável antes de qualquer push.

Achados por task (nenhum bloqueia o encerramento; nenhum é regra de negócio violada — todos são lacunas de teste, redundância ou aspereza de borda já cobertas por caminhos que falham fechado):

- **Task 1:** `pandas-stubs` fixado com piso enquanto ruff/mypy usam pin exato (pyproject); `test_boundary_scan_sees_every_pure_file` usa o número mágico `>=12` (mandado pelo plano).
- **Task 2:** fixtures de template/banco de dados do conftest vazam banco se o upgrade/clone falhar antes do `yield` (mandado pelo plano); `test_metadata_matches_migrated_columns` compara só nomes de coluna, não tipos/nullability; `vo_app` é uma role global de cluster compartilhada por todos os bancos de teste.
- **Task 3:** `parse_ts` aceita offsets não-UTC (só rejeita *naive*; toda escrita normaliza para UTC); o teste de `identity()` usa `event_key == type` (`"FILLED"`), então `event_key` vs `type` não fica fixado ali (testes de ledger posteriores usam chaves distintas).
- **Task 4:** `_pages` do Alpaca não tem teto de páginas (um `next_page_token` que se repetisse causaria loop).
- **Task 5:** ramo `SourceDataError` de `_quantize` é inalcançável (chamadores pré-filtram); `float()` redundante em `fetch_dividends`.
- **Task 6:** a deduplicação de ingestão compara só OHLCV, ignorando mudança de `provider`/`provider_version` (mandado pelo plano); `content_hash` cobre só as barras alteradas (intenção não documentada).
- **Task 7:** quarentena retorna `True` para ordens finais mesmo o congelamento sendo no-op, e `False` quando a projeção está ausente; `append_events` confia no `next_seq` do chamador (valor obsoleto vira `IntegrityError` bruto do banco, não quarentenado); `segment_containing` usa `.first()` sem `ORDER BY`; `get_run` levanta `NoResultFound` em vez de erro de domínio; falta teste de chave repetida dentro de um mesmo lote; teste de segundo incidente não verifica que `next_seq` permanece 4; `test_lock_order_blocks_concurrent_writer` pode travar se o assert falhar antes do commit (baseado em sleep); evento de mercado com `bar_ts` `None` é reportado como `EVENT_BEFORE_EVALUATION_START` com a mensagem "None precedes" (mandado pelo plano); o `get_order` de `record_incident`, dentro da transação de incidente, ainda substituiria o erro original se lançasse um erro de banco que não fosse `OrderNotFound`; o teste de falha de congelamento verifica a ordem das linhas de incidente sem `ORDER BY`.
- **Task 8:** `parse_signal_body` usa `cast(Decimal, ...)` sobre um dict misto de campos obrigatórios/opcionais; o teste de submissão concorrente não verifica que as threads terminaram após `join(timeout)`; a validação para em `MISSING_FIELD` antes de checar tipos (mandado pelo plano; relevante para os corpos 422 do Plano 3).
- **Task 9:** ~~uma exceção que não seja de integridade em uma ordem marca o run inteiro como `FAILED` e para as demais ordens~~ (resolvido na onda de correção final, ruling 15: vira `ERROR:<Type>` por ordem); uma exceção inesperada na ingestão escapa antes de `start_run` (sem linha de run/`FAILED`); exceções em `release_cycle_lock`/commit/`finish_run(FAILED)` dentro do `finally` podem mascarar o erro original (resultado do unlock não é checado); `market_now` *naive* é silenciosamente interpretado como hora local; o teste de ausência interna nunca ingere a barra tardia no banco (fraco); o teste de minuto não fechado só cobre o limite de busca, não o de leitura; falta teste de status `FAILED` do run + liberação do lock numa exceção no meio do ciclo.
- **Fix F1:** `_is_frozen_core_source` só trata `OSError` de `Path.resolve` (`ValueError`/`RuntimeError` ficam sem tratamento; praticamente inalcançável); falta teste fixando o comportamento de colisão de prefixo `src/core_extra`.
- **Task 10:** ~~`expire_due_orders` não tem isolamento por ordem~~ (resolvido na onda de correção final, ruling 15); o padrão lock/load/checagem-final está duplicado entre `commands.apply_command` e `cycle.evaluate_order`; a checagem `is_final` de `cancel_order` duplica a checagem de no-op do fill model.
- **Task 11:** exceções inesperadas dentro da transação de decisão desfazem o run `ACTIONABILITY` (sem registro `FAILED`); a ingestão só captura 3 tipos de exceção; os testes não verificam ausência de ordens em `UNVERIFIABLE`/falha de provider/política customizada, nem `actionability_data_as_of`, nem o caminho `UnknownDataSource`; teto redundante `min(..., valid_until_ts)`; o implementador moveu arquivos para `/tmp` durante o RED (restaurado).
- **Task 12:** ~~a comparação de `SEGMENT` não verifica `len(events) == segment.event_count`~~ (moot: a comparação da janela já diverge quando a contagem difere); faltam testes para divergência de `ORDER_CREATED`, `ORPHAN_SEGMENT`, `UNEXPECTED_EVENT_OUTSIDE_SEGMENT`, `SEQ_NOT_CONTIGUOUS`, replay de `FROZEN:INTEGRITY` e replay de `DIVIDEND` (a Task 13 acrescenta o rebuild de dividendos); `rebuild_all_projections` só captura `LedgerIntegrityError` (outras exceções param a varredura sem incidente); o teste de rebuild de correção de fornecedor não verifica que a correção foi gravada como nova versão.
- **Task 13:** as duas fontes de dividendo falhando ao mesmo tempo é silencioso (sem linha, sem revisão, sem log); só o primeiro split da janela é registrado (retorno antecipado do congelamento em `SPLIT`); erros da fonte de split se propagam (falha fechada, não documentada/testada); guarda genérica sugerida para `apply_result` (todos os eventos deduplicados mas o estado mudou → erro de integridade), não necessária nos caminhos atuais; faltam testes de débito SHORT, ambas as fontes falhando, `review_reasons` de reavaliação não verificada, janela de data UTC vs ET, e `now` *naive* → `TypeError`; falta teste fixando os ramos PENDING-com-dividendo intocado/sinalizado ou `qty_open==0` sem evento; falta teste de rerun/rebuild após `UNCONFIRMED`/`MISMATCH`; ~~ordens PENDING submetidas depois do fechamento anterior recebem falsos sinalizadores `DIVIDEND_POSITION_UNCONFIRMED`~~ (resolvido na onda de correção final: `evaluation_start_ts >=` abertura da ex-date não é candidata); um rerun após avaliação posterior pode creditar (a entrada mudou).
- **Task 14:** dados de referência são buscados mesmo para tickers cujas ordens já têm `DATA_QUALITY` (chamadas yfinance redundantes, entradas "indisponível" enganosas); não há lock em nível de job para jobs de fim de dia concorrentes (dois runs `END_OF_DAY` por pregão; saída ainda correta); as checagens de "touch" usam o estado da ordem no fim do dia, não por minuto (erra para mais sinalização; núcleo congelado); faltam testes de clamping multi-pregão, ordem congelada, fronteira do limiar de gap, D4 com versão revisada de uma barra existente; os testes de `ValueError` não verificam ausência de linha em `evaluation_runs`; o teste de projeção ausente aceita contagem de incidente `>=1`, que uma corrida anterior também satisfaria; `market_now` *naive* é interpretado como hora local; a expiração/qualidade em `run_end_of_day` roda depois do lock do ciclo ser liberado (um ciclo concorrente é possível).
- **Task 15:** divergência numa fonte sem projeção registra incidente mas não congela (comportamento da quarentena; o relatório não avisa disso); replay de um replay é permitido, e `replay_of_order_id` aponta para o replay intermediário (a spec é omissa — decidir na revisão final); o detail do run `RUNNING` guarda todos os ids de fonte (grande para intervalos largos); faltam testes verificando barras corrigidas/tardias visíveis, tipo/detail do incidente, evento `FROZEN:INTEGRITY`, `price_source`/janela de avaliação copiados, detail do run `REPLAY` `COMPLETED`, e ausência de incidente para ordem desconhecida; a projeção é construída a partir do `seq` local em vez de verificar `== history.next_seq`.
- **Task 16:** o override de `dividend_tolerance` é ignorado pelo RECALCULATE (confia no valor validado já gravado; sem guarda de divergência — documentar ou rejeitar a chave de override); dividendos não fazem parte do manifesto do snapshot (dois RECALCULATEs sobre o mesmo snapshot podem diferir em dividendos); `config_overrides` não é validado antecipadamente (chaves desconhecidas falham por fonte; um `float` falha a chamada inteira); os runs de trecho são marcados `COMPLETED` antes do próprio trabalho (uma única transação); sem restrição de unicidade em snapshots; o fim de validade não é condicionado a um pregão fechado (`data_as_of` logo após `valid_until` → `STALE_EXIT_BAR`); faltam testes de hash do RECALCULATE diferente + rebuild no teste de D6, múltiplos/não validados/dividendo no primeiro pregão, e ausência de linhas órfãs numa fonte que falhou; `recalculate_order` (singular, público) não tem guarda própria de timezone/watermark, depende de `recalculate_orders`.
