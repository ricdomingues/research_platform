# Plano 4 (Relatório diário de observação em paper) — Encerramento

- **Plano:** `docs/superpowers/plans/2026-09-14-paper-observation-report.md`
- **Spec:** v1.2 (`spec/virtual-order-engine-v1.2`) + D5–D59 (Planos 2, 3A, 3B, 3C) + D60–D73 (este plano)
- **Intervalo:** `09f2eea..4fc735b` (base `main`) — 18 commits (17 de implementação, correções de revisão e testes + este encerramento)
- **Testes (Step 2, do zero, no commit `4fc735b`):** raiz 1372 passando, 0 skips, 0 warnings (`N_BASE` 1128 herdados do Plano 3C); dashboard 56 passando, 0 skips, 0 warnings; a chamada única que mistura raiz, `analytics`, `readmodels`, `integration`, `api`, `worker` e `deploy` passa com 680 testes; `migrations from an empty database: OK` (upgrade/downgrade/upgrade, base vazia).
- **Núcleo congelado:** `git diff plan/virtual-order-engine-core-complete -- src/core` vazio; `git status --porcelain -- src/core` vazio.
- **Locks e CI:** os dois `uv.lock` (raiz e `dashboard/`) e `.github/workflows/ci.yml` sem diff contra `09f2eea`.
- **ruff e mypy:** limpos nos dois projetos (121 arquivos na raiz, 16 no dashboard).

Todos os nomes de teste citados abaixo foram conferidos com `grep -n "def <nome>"` sobre `tests/` e `dashboard/tests/` nesta sessão: as 47 funções da tabela de critérios de aceite existem, cada uma com exatamente uma definição, sem nenhum nome divergente do brief — nenhum ajuste foi necessário.

## Critérios de aceite

| # | Critério (métrica do responsável ou invariante) | Teste(s) | Resultado |
|---|---|---|---|
| 1 | **Falhas de provider** por tipo de run, com códigos fixos, maior sequência LIVE e sem mensagens | `test_provider_failures_are_counted_by_run_kind_with_codes_and_never_messages` (`tests/integration/test_observation_operations.py`); `test_failure_messages_become_fixed_codes` (`tests/analytics/test_observation.py`); `test_the_report_is_read_from_stored_data_only_and_labels_the_estimate` (`tests/integration/api/test_observation_api.py`) | OK |
| 2 | **Candles ausentes**: minutos esperados/ausentes do pregão, `DATA_GAP`, não avaliadas | `test_missing_bars_gaps_and_unevaluated_orders_come_from_the_session_quality` | OK |
| 3 | **503 de actionability** com causas e taxa | `test_actionability_answers_are_counted_with_the_503_causes` | OK |
| 4 | **`DATA_QUALITY_RECHECK`**: runs, linhas, status, motivos terminais, linhas sobre o pregão | `test_recheck_rows_are_attributed_to_the_recheck_runs_of_the_window` | OK |
| 5 | **Falhas de entrega de alertas** por desfecho e tipo, pendentes no fim | `test_alert_deliveries_are_counted_by_the_database_clock_inside_the_window` | OK |
| 6 | **Reinícios do worker**: `worker_sessions` append-only; runner grava início/parada sem mudar códigos de saída; falha de escrita não para o worker; reinícios, fins sem parada atribuídos à janela do próximo início, sessão aberta no fim, `LOCK_LOST` limpo | `tests/integration/test_schema_0005.py` (todos); `test_a_session_records_its_start_and_its_signalled_stop`, `test_a_lost_lock_is_recorded_with_exit_code_3`, `test_a_failing_scheduler_is_recorded_and_still_raises`, `test_a_worker_that_never_gets_the_lock_is_not_a_session`, `test_a_failing_session_write_never_stops_the_worker_and_logs_only_the_type` (`tests/integration/worker/test_runner.py`); `test_worker_restarts_unclean_ends_and_open_sessions` | OK |
| 7 | **Transições de saúde** (mudanças de estado, com `log_rows` à parte), tempo por estado e as-of | `test_health_transitions_and_time_in_each_state`; `test_seconds_by_state_splits_the_window_at_each_change` | OK |
| 8 | **Trades virtuais**: criadas, fills, fechadas, política de revisão, replay fora | `test_trades_latency_and_unfilled_orders_of_a_session` (`tests/integration/test_observation_trades.py`); `test_the_report_is_read_from_stored_data_only_and_labels_the_estimate` (`replay_closed`) | OK |
| 9 | **MFE / MAE** da projeção, médias e medianas | `test_trades_latency_and_unfilled_orders_of_a_session`; `test_trade_stats_follow_the_r_sign_and_median_rules` | OK |
| 10 | **Latência sinal → fill** (candle e gravação), mediana/p90 nearest-rank, sem fill por desfecho | `test_trades_latency_and_unfilled_orders_of_a_session`; `test_nearest_rank_percentiles_never_interpolate`; `test_whole_seconds_floors_and_rejects_naive_datetimes` | OK |
| 11 | **Pressão × resultado**: últimos 30 candles antes do sinal atravessando pregões (janela e `spans_sessions` gravados), buckets por alinhamento/força, média só com n ≥ 5, sempre rotulada, nunca lida pela avaliação | `test_pressure_is_recomputed_from_bars_before_the_signal_and_paired_with_the_result`; `test_an_early_session_signal_reads_the_last_bars_of_the_previous_session`; `test_pressure_is_classified_relative_to_the_trade_direction`; `test_buckets_group_results_in_a_fixed_order_and_need_five_trades_for_a_mean`; `test_evaluation_never_reads_the_observation_report` (`tests/test_import_boundaries.py`) | OK |
| 12 | Janela do pregão, dias sem pregão e futuro rejeitados, resumo de intervalo | `tests/readmodels/test_observation_window.py` (todos); `test_the_summary_aggregates_the_sessions_of_the_range`; `test_invalid_report_days_use_fixed_codes`, `test_invalid_summary_ranges_use_fixed_codes`, `test_the_summary_covers_every_session_of_the_range` | OK |
| 13 | Somente leitura: nenhuma escrita e nenhuma chamada a provider no relatório, na rota e na CLI; um único snapshot `REPEATABLE READ` somente leitura por pedido | `test_the_report_assembles_every_section_for_one_session_without_writing`; `test_the_report_reads_one_read_only_repeatable_read_snapshot`; `test_the_report_is_read_from_stored_data_only_and_labels_the_estimate`; `test_report_prints_markdown_or_json_and_never_writes` | OK |
| 14 | CLI: Markdown/JSON, códigos de saída 0–4 (`4` só para erro de conexão), sem segredos | `test_report_prints_markdown_or_json_and_never_writes`, `test_summary_prints_one_row_per_session`, `test_exit_codes_carry_codes_and_types_only`, `test_only_connection_errors_are_database_unavailable`; `test_the_observation_cli_never_imports_adapters_the_composition_root_or_the_http_layer` | OK |
| 15 | Alerta `OBSERVATION_DAILY`: só contagens, um por pregão, só com sink, contido, log só com o tipo | `test_end_of_day_enqueues_one_observation_alert_per_session_with_counts_only`, `test_no_observation_alert_without_a_webhook`, `test_a_failing_observation_alert_never_fails_the_end_of_day_job` (`tests/integration/worker/test_jobs.py`) | OK |
| 16 | Dashboard "Observação" só por HTTP: cliente, view-models, figuras, página, erro 422 sem traceback, pressão rotulada | `test_observation_routes_send_iso_dates` (`dashboard/tests/test_client.py`); `test_the_observation_view_turns_every_section_into_display_strings`, `test_summary_rows_and_the_observation_error_message` (`dashboard/tests/test_viewmodels.py`); `test_daily_r_and_pressure_bucket_figures` (`dashboard/tests/test_charts.py`); `test_observation_page_labels_the_pressure_estimate_and_shows_the_summary`, `test_observation_page_shows_an_invalid_day_without_a_traceback` (`dashboard/tests/test_app_smoke.py`) | OK |
| 17 | Contrato gravado das rotas novas (19 fixtures), determinístico e sem segredo | `test_dashboard_contract_matches_the_recorded_api_responses`; `test_the_client_reads_the_recorded_observation_responses`, `test_observation_views_from_the_recorded_responses` (`dashboard/tests/test_api_contract.py`); `test_boundary_scan_covers_the_3c_modules` (19) | OK |
| 18 | `src/core` intocado; `N_BASE` herdados verdes; migrations do zero; ruff e mypy strict nos dois projetos; locks e CI inalterados | Task 13 Step 2 | OK — ver cabeçalho |
| 19 | Commits com identidade noreply e sem trailers de IA; nenhuma tag com revisão aberta; nenhum push | Task 13 Steps 2 e 6 | OK — 18 commits, autor único `Ricardo Carneiro <132141856+ricdomingues@users.noreply.github.com>`, `no AI trailers`; nenhuma revisão aberta (re-revisão do fix wave: todos os 8 achados ADDRESSED) |

## Decisões D60–D73

- **D60** (relatório neutro, só leitura, montado de dados já gravados; nada recalculado por provider) — mantida — custo se errada: o relatório criaria efeito colateral (run, evento) só por ser lido; impedido pelas contagens antes/depois das Tasks 7 e 8.
- **D61** (janela do pregão + pertinência pelo instante de mercado ou pelo relógio do banco; `as_of` sob o lock de ingestão; um snapshot `REPEATABLE READ` somente leitura por pedido; exceção de `order_state`) — mantida — custo se errada: um fato de fim de semana sumiria ou contaria duas vezes, ou seções do mesmo relatório ficariam em desacordo entre si (`trades.filled` ≠ `latency.bar.count`).
- **D62** (`worker_sessions` append-only, migration `0005`; escrita melhor esforço; atribuição exata de reinícios e fins-sem-parada à janela do próximo início; `LOCK_LOST` sempre conta como fim limpo, mesmo gravado depois do início do sucessor) — mantida — custo se errada: uma escrita que travasse o worker derrubaria a avaliação, impedido pelo teste de falha de escrita; nuance não escrita no texto da própria decisão: um `STOPPED` **não**-`LOCK_LOST` gravado depois do início do sucessor também conta como sujo (ver entrada 8 do próximo plano).
- **D63** (validação de `/observation/report` e `/observation/summary` com códigos fixos e `MAX_SUMMARY_DAYS = 45`; CLI reusa as mesmas funções) — mantida — custo se errada: um dia inválido ou um intervalo grande demais seria aceito em silêncio, ou rejeitado com o código errado.
- **D64** (trades virtuais e MFE/MAE seguem a política de revisão da spec 5.4; replay isolado em `replay_closed`) — mantida — custo se errada: as métricas de trades misturariam ordens de replay com reais, ou incluiriam ordens sob revisão nas estatísticas.
- **D65** (latência sinal → fill por nearest-rank sem interpolação; ordens sem fill contadas por desfecho as-of; `MANUAL_USER` também a partir do `created_at` do sinal) — mantida — custo se errada: a latência ficaria sub/superestimada, ou a população de `unfilled` seria inconsistente entre chamadas do mesmo relatório.
- **D66** (pressão recomputada as-of do relatório sobre os últimos 30 candles atravessando pregões, rejeitando a regra "data ET do último candle" da D44; nunca gravada; nenhum módulo de avaliação a importa) — mantida — custo se errada: sinais do início do pregão ficariam enviesados para `INSUFFICIENT_BARS` pela hora do sinal, ou um candle corrigido pelo vendor depois do sinal mudaria a estimativa sem aviso (aceito e rotulado "as-of do relatório, não do sinal").
- **D67** (falhas de provider por tipo de run, com regex fixo de tipo de exceção e maior sequência `LIVE` dentro da janela, descritiva e distinta da regra do `/health`) — mantida — custo se errada: texto de mensagem/segredo vazaria, ou a métrica seria confundida com a regra operacional do `/health`.
- **D68** (saúde, actionability, qualidade, recheck e alertas com definições fixas por seção; `transitions` separado de `log_rows`; `about_this_session` sem dupla contagem com `rows`) — mantida — custo se errada: uma pendência de qualidade sumiria da janela, ou uma transição de saúde seria contada duas vezes.
- **D69** (resumo de vários pregões recalcula cada relatório no mesmo snapshot, sem cache; `window_runs` varre uma vez por relatório, sem limite inferior) — mantida — custo se errada: até 45 relatórios por pedido ficariam lentos ou incoerentes entre si sem o snapshot único; aceito para 7–14 dias (ver entrada 6 do próximo plano para histórico maior).
- **D70** (rotas aditivas; CLI com códigos de saída 0–4; alerta `OBSERVATION_DAILY` só com contagens, idempotente por pregão, "o primeiro enfileiramento vence") — mantida — custo se errada: um pregão perderia o relatório por todas as vias, ou o alerta vazaria ticker/id/texto de mensagem.
- **D71** (página "Observação" só por HTTP, view-models puros, mensagem fixa para `OBSERVATION_REQUEST_INVALID`) — mantida — custo se errada: o dashboard chamaria a API fora de `client.py`, ou mostraria texto de exceção cru na tela.
- **D72** (contrato gravado com as 19 fixtures — 17 antigas byte-idênticas + 2 novas determinísticas —, latência por `recorded_at` fixada em 0 na canonicalização) — mantida — custo se errada: uma mudança de forma na API quebraria o dashboard em produção sem nenhum teste acusando.
- **D73** (CLI sem `load_settings`/`build_services`; só `DATABASE_URL` e `make_engine`; nenhum import de provider) — mantida — custo se errada: a CLI de leitura exigiria chaves de provider desnecessárias para um relatório só-leitura, ou poderia acidentalmente chamar um provider.

## Rulings durante a execução

1. **Task 0 não despachada.** `N_BASE = raiz 1128 / dashboard 47`, 0 skips, 0 warnings, medido do zero em `1e2d6de` (verificação final do Plano 3C) e verde no CI em `main` `09f2eea`; `e77dec5` difere de `09f2eea` só por documentação do plano, então a base efetiva do Plano 4 é a mesma árvore. Custo se estivesse errada: a linha de base da Task 1 ficaria errada, mas a própria corrida completa da Task 1 pegaria a discrepância.
2. **Follow-ups da revisão do plano.** O regex de tipo de erro foi restrito a `^[A-Z][A-Za-z0-9_]*(?=\()` (prefixos com ponto, como `db.internal(host)`, vazavam fragmento de mensagem); buckets de pressão com `n < 5` não são desenhados (ficam sem média, rotulados). Custo se estivesse errada: menos detalhe de tipo de erro para exceções incomuns.
3. **Task 4 — controlador committou o código do implementador travado.** O implementador travou três vezes (stream watchdog, a primeira com ~10 min ocioso e ~950 linhas não rastreadas sem commit); o controlador rodou os testes focados com guarda de 240s (18 passando em 1,95s, descartando teste travado), conferiu ruff/mypy limpos (114 arquivos) e, na terceira parada, commitou o código já verificado verbatim contra os blocos do brief (`c3b6966`; 1198/0/0). Custo se estivesse errada: não haveria evidência RED própria do controlador — a revisão opus da task era o portão de segurança real, e passou limpa.
4. **Task 13 Step 1 — mesmo padrão.** O implementador (haiku) também travou; o controlador verificou o acréscimo puro (`test_boundary_scan_covers_the_plan_4_modules`) verbatim contra o bloco do brief, 448 testes de fronteira passando, raiz 1247/0/0, dashboard 56/0/0, ruff/mypy limpos, sem trailers, e commitou diretamente (`026ab7c`) — sem uma revisão de task isolada, sua correção foi conferida dentro da revisão final de branch inteiro (que a diffou linha a linha contra o plano). Custo se estivesse errada: um append incorreto só apareceria na revisão final — que de fato cobriu o commit especificamente por esse motivo.
5. **Task 6 — rodada de correção única.** Um Important do controlador (o teste de fronteira engolia o último assert da cobertura de módulos do 3C) foi corrigido junto com minors baratos: mover a cópia local de `ACTIONABILITY_UNVERIFIABLE` para uma fonte neutra (ou fronteira reversa `readmodels/observation*` vs `ledger.writes`/`ledger.runs`/`marketdata.ingest`), teste de resumo vazio levantando exceção, teste de conexão RR não somente-leitura recusada. Custo se estivesse errada: nenhum.
6. **Task 8 — Important mandado pelo plano, mais verificação direta do controlador.** O ramo `INVALID:DATABASE_URL` estava sem teste; corrigido na hora com um teste (`engine_factory` levantando exceção; saída 2; texto da URL ausente da saída) e reuso de `APPLICATION_LAYER` no teste de fronteira da CLI. O controlador verificou a correção de 20 linhas diretamente (diff lido, 449 testes cobrindo passando, sem trailers) em vez de abrir uma vaga de re-revisão. Custo se estivesse errada: a revisão final veria a lacuna — o que de fato era o plano B.
7. **Revisão final — rodada de correção = 8 itens combinados.** I1 (o escâner de fronteiras não via `from pkg import submodule` nem imports relativos — promovido a Important porque é o único mecanismo que garante o invariante da D66 "avaliação nunca importa o relatório"), M3 (`# type: ignore[index]`), M4 (ramo inalcançável em `pressure_before`), M5 (teste de candle corrigido para a janela de pressão), mais os baratos M1 (`as_of` faltando na primeira LATERAL de `_WINDOW_RUNS`), M2 (atribuição de um `STOPPED` tardio de sessão iniciada duas ou mais vezes atrás), M6 (guardas de entrada do resumo: `as_of` único e ordenação por `session_day`), M9 (`COUNT DISTINCT` em vez de `COUNT(*)` em `orders_with_missing`) — todos pequenos, sem mudança de comportamento sobre os dados atuais. Custo se estivesse errada: mais um commit pequeno de correção.
8. **Revisão final — itens que vão para o próximo plano, não correção agora.** M7 (consulta de pressão por trade fechado dentro de um snapshot RR longo, mais a varredura sem limite inferior de `window_runs`, em escala maior) e M10 (`MAX_SUMMARY_DAYS` duplicado no dashboard sem checagem de paridade) e a nuance da D62 (um `STOPPED` não-`LOCK_LOST` gravado depois do início do sucessor conta como fim sujo — comportamento correto, mas ausente do texto da própria decisão) foram registrados como entradas do próximo plano em vez de corrigidos agora. Custo se estivesse errada: o responsável só encontraria esses efeitos com histórico de paper maior do que 7–14 pregões.

## Resultado da revisão final

Revisão de branch inteiro (opus, `09f2eea..026ab7c`, somente leitura — o controlador é quem roda a verificação do zero): **Ready to merge — com correções.** 0 Critical, 1 Important (I1), 10 Minor (M1–M10).

Rodada de correção única (commit `4fc735b`, um só commit para os 8 itens ruled): I1, M3, M4, M5, M1, M2, M6, M9. Antes de tocar qualquer código de produção, a suíte completa de fronteiras rodou com a mudança de `_imported_modules` isolada: 569 passando, 0 falhas — nenhuma violação pré-existente revelada pelo aperto do escâner. Raiz: 1372 passando, 0 skips, 0 warnings (+125 sobre `1247`: 121 novas parametrizações `test_src_has_no_relative_imports`, uma por arquivo de `src/`, mais 4 testes novos — 2 para M6, 1 para M2, 1 para M5). Dashboard não tocado (nenhum arquivo de `dashboard/` mudou), suíte/ruff/mypy do dashboard não re-rodados nesse commit.

Step 2 refeita do zero em `4fc735b` (Task 13 Step 2 re-run): `uv sync --locked` inalterado nos dois projetos; raiz 1372 passando, 0 skips, 0 warnings; ruff e mypy limpos (121 arquivos); dashboard 56 passando, 0 skips, 0 warnings, ruff/mypy limpos (16 arquivos); a chamada única mista (fronteiras + `analytics`/`readmodels`/`integration`/`api`/`worker`/`deploy`) passa com 680 testes; `migrations from an empty database: OK`; diffs de `src/core`, dos dois locks e do `.github/workflows/ci.yml` todos vazios contra `09f2eea`/o núcleo congelado; 17 commits até esse ponto, autor único noreply, `no AI trailers`.

Step 3 smoke refeito em `4fc735b`: 24 rotas conforme esperado; CLI sem config → `exit=2` (`CONFIG_INVALID`/`MISSING:DATABASE_URL`); CLI com banco inalcançável → `exit=4` (`DATABASE_UNAVAILABLE`/`OperationalError`, sem segredo — `hunter2` ausente da saída); worker sem config → `exit=2`; view de observação do dashboard importa (`render`, sem servidor Streamlit e sem chamar a API).

Re-revisão de escopo restrito (sonnet, sem testes novos): os 8 achados marcados **ADDRESSED**, nenhuma quebra nova. Preservação de resultado de M1 e M9 provada a partir do código do escritor e das restrições `UNIQUE` (não apenas re-executando os testes); dupla contagem de M2 provada estruturalmente impossível pela combinação `UNIQUE(session_id, event)` + dicionário indexado por `session_id`.

## Achados menores adiados

Descartados (drop), triagem item por item da revisão final:
- Task 2 — o relatório do implementador atribuiu +4 à linha de base por engano (casos de fronteira do módulo novo; um `stash` deixou um arquivo não rastreado): bookkeeping da medição, não do código; a árvore ficou limpa e a contagem foi re-medida.
- Task 2 — fingerprint real do hostname escrito em testes sem `hostname=`: é um prefixo SHA-256, nunca o nome, e há teste dedicado (`test_host_fingerprint_is_a_short_hash_and_never_the_name`) pinando isso; os dois testes de sessão que importam já passam `hostname=`.
- Task 4 — comentário sobre a ordenação de empate no EOD: cosmético, `completed[-1]` por `(instant, run_id)` já é determinístico.
- Task 5 — falta de teste de ordem de replay para `replay_closed`/exclusões: agora coberto por `test_the_report_is_read_from_stored_data_only_and_labels_the_estimate`, que afirma `(closed, replay_closed, filled) == (2, 2, 2)` sobre um replay `REPRODUCE` real, com o valor também pinado na fixture gravada.

Adiados (defer) para o próximo plano ou para quando o código for tocado de novo:
- Task 2 / M8 — `BaseException` dentro de `_stop_session` pularia `release_worker_lock`/`services.close()`: mandado pelo brief; impacto praticamente nulo porque o Postgres libera o lock consultivo ao desconectar.
- Task 4 — linha de `STOPPED` gravada depois da meia-noite para uma sessão substituída, sem dono claro: mesma causa raiz do M2; resolvida junto com a correção geral do M2 (união de `stops` por `recorded_at` além de por sessão de início), não é mais uma pendência isolada.
- Task 4 — valor malformado de `detail` armazenado quebraria o relatório: os casts `::int`/`::timestamptz` em `_SESSION_QUALITY`/`_SESSION_GAPS` levantariam, mas todo escritor é nosso e a falha é alta e contida (CLI saída 1 `DATABASE_ERROR`, rota 500 sem texto).
- Task 4 — lacunas de cobertura (chaves de falha de actionability/recheck, intercalação de sequência, stop depois de `effective_end`): reais mas de segundo grau; o caso de fatos de fim de semana já está coberto por `test_a_session_window_owns_the_days_without_a_session_before_it` e pelas fixtures de Ação de Graças — só a intercalação/chaves de falha seguem sem teste.
- Task 5 — corte no minuto exato do sinal e `price_source` diferente sem teste próprio: `floor_minute` + `ts < :cutoff` é exercitado indiretamente pelo teste das 09:45 ET; `price_source` é um bind simples sem lógica própria.
- Task 5 — possível `r_multiple` `NULL` sem guarda explícita: `_CLOSED` exige `status='CLOSED'` com um evento de fechamento gravado, o que implica `r_multiple` calculado; um `NULL` apareceria como `TypeError` alto e visível, não como um número errado silencioso.
- Task 6 — resumo confia na ordem/`as_of` de sua entrada: a metade `_imported_modules` desta linha do ledger virou I1 (promovida a Important) e foi corrigida no fix wave; a metade "resumo confia na ordem/`as_of`" virou M6 e **também** foi corrigida no mesmo fix wave (`build_observation_summary` agora rejeita `as_of` não-único ou `session_day` fora de ordem, com dois testes novos). Mantida aqui só por rastreabilidade da triagem original — não é mais uma pendência aberta.
- Task 10 — fixação de latência por relógio de parede (`_pin_wall_clock_latency`) é um segundo mecanismo de normalização além do `Normalizer`: documentado no próprio docstring e na D72; consolidar num único mecanismo é um refactor sem mudança de comportamento.

## Entradas para o próximo plano

1. Rodar a observação por 7–14 pregões e decidir, com os números do relatório: D33 (colapso de revisões), burst de cruzamentos (D26), T12, `NO_OBSERVATIONS_FINAL` sem revisão (D52), finalização na primeira tentativa (Minor 4 do 3C).
2. Limiares da pressão × resultado (`0.05`/`0.15`, janela de 30 candles): rever só com amostra suficiente; nunca promover a sinal sem plano próprio.
3. `window_runs` varre todos os runs dos tipos pedidos (sem limite inferior, porque o instante de mercado vive no `detail`): aceitável para semanas; com meses de histórico, gravar o instante de mercado em coluna própria ou limitar por `started_at` com folga.
4. O alerta `OBSERVATION_DAILY` é a fotografia do fim de dia (`complete: false`); se o responsável quiser o dia fechado no n8n, um job às 00:05 ET.
5. Entradas 1, 3, 4, 6–11 do encerramento do 3C continuam abertas (fora deste plano).
6. **M7** (revisão final): um único snapshot `REPEATABLE READ` longo, somente leitura, mais uma consulta de pressão por trade fechado (até 45 relatórios × trades fechados num pedido de resumo), e a varredura sem limite inferior de `window_runs` (entrada 3 acima) — em escala maior de histórico, a combinação segura o horizonte `xmin` de um banco append-only por mais tempo; avaliar limitar por `started_at` ou mover a consulta de pressão para fora do snapshot longo.
7. **M10** (revisão final): `MAX_SUMMARY_DAYS` está duplicado como constante no dashboard (`dashboard/dashboard/views/observation.py`) sem checagem de paridade com o valor real da D63 na API; se o limite mudar na API, a página continuaria oferecendo um intervalo que a API rejeita com `RANGE_TOO_LARGE`. Guarda mais barata: expor o limite na resposta do resumo e usá-lo para limitar o widget, ou afirmá-lo no teste de contrato gravado.
8. Nuance da D62 não escrita no texto da própria decisão: um `STOPPED` **não**-`LOCK_LOST` gravado depois do início do sucessor também conta como fim sujo (`unclean_ends`). O comportamento está correto e é coberto por teste; falta só documentar a frase na D62 quando o texto do plano for revisitado.
9. Achados menores adiados que seguem em aberto (ver seção acima para o detalhe de cada um): Task 2 — `BaseException` dentro de `_stop_session`; Task 4 — valor malformado de `detail` armazenado e lacunas de cobertura (intercalação de sequência, chaves de falha de actionability/recheck); Task 5 — corte no minuto exato do sinal, `price_source` diferente e `r_multiple` `NULL` sem guarda explícita; Task 6 — a metade "resumo confia na ordem/`as_of`" da linha original (já corrigida pelo M6 no fix wave; mantida aqui só por rastreabilidade); Task 10 — a fixação de latência por relógio de parede como segundo mecanismo de normalização além do `Normalizer`.
